# Phase 46 — Karts-Tab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eigener Karts-Tab (analog Fahrer-Tab) mit einer Karte pro Kart — Identität, Live-Status, Motorstunden/Service, Kalibrierung — auf Basis eines vereinheitlichten, persistenten Kart-Rosters; die drei bisherigen Verwaltungsstellen (✏-Popover, Verbindungs-Tab-Kartliste, Settings-Motor-Zeilen) entfallen.

**Architecture:** Neues pures Modul `src/kart-roster.js` (node-testbar) trägt die Roster-Mechanik (Defaults, Legacy-Migration, Sortierung); rasicross.js erweitert `_persistedKarts` um `meta` (SAVE_KEY additiv: Feld `kartsMeta`) und exportiert Accessoren; neues UI-Modul `src/karts-page.js` rendert die Karten und übernimmt Forget/Service/Kalibrier-Aktionen. kart-bar.js delegiert `metaFor` an das Roster und verliert Editor + ✏.

**Tech Stack:** Vite/ESM (Phase 42), node:test (ESM in test/), Playwright-Smoke (RasiTest-Brücke), kein neues npm-Paket.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-07-karts-tab-design.md` (alle Entscheidungen darin sind bindend).
- **Gate:** `npm test` grün (178 + neue kart-roster-Tests), `npm run lint` 0, `npm run test:e2e` grün (6 bestehende + 3 neue Karts-Tests), Portable-EXE-Sichtprüfung.
- **Voraussetzung:** Branch `feat/phase-46-karts-tab` existiert (gestapelt auf `feat/phase-42-vite-esm`, PR #66). Nach Merge von #66 rebased GitHub die PR-Basis automatisch.
- `SAVE_KEY`-Format nur additiv (`kartsMeta` neu); `REC_VERSION` 9.6, serielles Protokoll, `bridge.py`/`sender.py`/`esp_libs/` unangetastet.
- Demo-Karts (`DE:MO:*`) werden nie persistiert (weder cal/eng — bestehend — noch meta).
- Globale Sensorik-Regler (setInvertGx/Gy, setSwapG, setInvertYaw, setInvertRollRate, Drift-/Rollover-Settings) bleiben unverändert im Einstellungen-Tab.

## Working Directory & Conventions

- Arbeitsverzeichnis `C:/Users/jimlu/Documents/RasiCross-Telemetrie-git`; Branch `feat/phase-46-karts-tab`.
- Dateien sind **CRLF**: Ziel-Region unmittelbar vor jedem Edit frisch lesen, Anker aus dem frischen Read kopieren. Verifikation mit dem Grep-Tool. Zeilennummern in diesem Plan sind Stand Plan-Erstellung — IMMER frisch lesen.
- Niemals `.claude/`, `CLAUDE.md`, `graphify-out/` committen. Commit-Messages ohne Anführungszeichen, mit Pflicht-Trailern (Co-Authored-By + Claude-Session).
- ESLint ist das Import-Orakel: `npx eslint src/<datei>.js` nach jeder Modul-Änderung; jedes no-undef = fehlender Import, jedes no-unused-vars = toter Import.
- `graphify update .` erst am Phasenende (Task 8).

## Locked Decisions

- **Pure Logik in `src/kart-roster.js`:** rasicross.js ist unter node:test nicht importierbar (Top-Level-`init()`, DOM). Deshalb Default-Objekt `RasiKartRoster` mit `PALETTE`, `isDemoMac`, `metaDefaults`, `ensureMeta`, `migrateLegacyMeta`, `rosterMacs`, `clampServiceH`, `calDefaults`. Die Kalibrier-Defaults duplizieren bewusst `kart-registry.makeKartState().calibration` (kart-registry bleibt unangetastet; Kommentar verweist aufeinander).
- **Meta-Maps:** echte MACs → `_persistedKarts.meta` (persistiert als `kartsMeta` im SAVE_KEY); Demo-MACs → modulinternes `_demoMeta` (Session-only). `kartMetaFor(mac, idx)` wählt die Map selbst.
- **Legacy-Migration:** eigene Funktion `migrateLegacyKartMeta()` in rasicross, von `init()` DIREKT NACH `loadData()` gerufen (läuft damit auch bei frischem SAVE_KEY): wenn `_persistedKarts.meta` leer und `localStorage['rasi.kartMeta.v1']` vorhanden → Einträge übernehmen, danach `removeItem`. Korrupter Alt-Key wird ignoriert (kein removeItem, kein Crash).
- **`RasiKartBar.metaFor(state, mac, idx)`-Signatur bleibt** (Konsumenten pit-wall/live-ui/kart-overview unverändert); delegiert intern an `kartMetaFor(mac, idx)`. `state.kartMeta` entfällt ersatzlos.
- **Umzüge:** `forgetKart` zieht von kart-bar.js nach karts-page.js; `resetKarts` („Alle Karts zurücksetzen") von pit-wall.js nach karts-page.js. `updateEngineUi` entfällt komplett (Settings-Zeilen weg; die Kart-Karte rendert Motorwerte selbst).
- **Offline-Karts:** Aktionen brauchen Zugriff auf die persistierten Ablagen ohne Registry-Bucket → rasicross exportiert `kartCalFor(mac)` / `kartEngineFor(mac)` (live-Bucket bevorzugt, sonst `_persistedKarts.cal/eng[mac]`, sonst `null`). karts-page mutiert die gelieferten Objekte und ruft `saveData()`/`saveDataDebounced()`.
- **Refresh-Strategie:** `setupTabs()`-Hook (`if (tab === 'karts') renderKartsTab();`) + 1-Hz-Refresh im bestehenden 1000-ms-Intervall von `initLiveUiLoops()` (nur wenn `document.body.dataset.tab === 'karts'`). Rebuild wird übersprungen, solange ein `<input>` innerhalb der Kartenliste den Fokus hat (Tipp-Schutz, ersetzt kart-bar-Focus-Restore-Mechanik).
- **ESM-Zyklus** live-ui → karts-page → kart-bar → live-ui ist zulässig (nur Top-Level-Deklarationen, dokumentierte Konvention).
- **`lastSeenAt`** wird im Kart-Paketpfad gepflegt (bei `k.connection.lastPacketAt = Date.now()`), nicht persistiert pro Paket — der nächste reguläre `saveData()` nimmt es mit.
- **RasiTest** erhält zusätzlich `updateKartMeta` und `kartRosterMacs` (e2e-Seed + Assertions).
- **Kein pw-hero** auf der Karts-Seite (YAGNI) — nur `pw-library`-Kopf mit Zähler + Reset-Button; Karten-CSS nutzt die vorhandenen `.dstat`-Kacheln.

## File Structure

| Action | Path | Responsibility |
|---|---|---|
| Create | `src/kart-roster.js` | pure Roster-Logik (Defaults, Migration, Sortierung, Klemmen) |
| Create | `test/kart-roster.test.js` | node:test für kart-roster |
| Create | `src/karts-page.js` | Karts-Tab-Rendering + Karten-Aktionen (Forget, Service, Kalibrier-Reset, Reset-alle) |
| Create | `e2e/karts.spec.js` | Smoke: Demo-Karten, Rename→Chip, Aktiv-Wahl, Offline-Persistenz |
| Modify | `src/rasicross.js` | meta-Map, Accessoren, Migration, lastSeenAt, Forget; updateEngineUi/Service-Wiring raus |
| Modify | `src/kart-bar.js` | metaFor delegiert; Editor/Forget/LS_KEY/✏ raus |
| Modify | `src/pit-wall.js` | renderConnKartList/resetKarts raus |
| Modify | `src/live-ui.js` | 1-Hz-Hook für Karts-Tab; updateEngineUi-Aufruf raus |
| Modify | `src/settings.js` | 4 SETTINGS_INDEX-Einträge raus (engineHoursText, setServiceIntervalH, engineSinceServiceText, serviceDoneBtn) |
| Modify | `src/app.js` | Import karts-page; RasiTest-Erweiterung |
| Modify | `index.html` | Nav-Item, `#tab-karts`, `.kart-card`-CSS; Popover/connKartList/Settings-Zeilen raus |
| Commit | `docs/superpowers/plans/2026-07-07-46-karts-tab.md` | dieses Plan-Doc (nur im finalen Task) |

**Task-Reihenfolge:** 1 (kart-roster pure, TDD) → 2 (rasicross-Roster-Integration) → 3 (kart-bar delegiert) → 4 (Karts-Tab-Gerüst: HTML/CSS/Render/Verdrahtung) → 5 (Karten-Aktionen + Umzüge) → 6 (Altstellen entfernen) → 7 (RasiTest + e2e, Smoke-Gate) → 8 (finale Gates + EXE + Plan-Doc + PR).

---

### Task 1: `src/kart-roster.js` (pure Logik, TDD)

**Files:**
- Create: `src/kart-roster.js`
- Test: `test/kart-roster.test.js`

**Interfaces:**
- Produces: `export default { PALETTE, isDemoMac, metaDefaults, ensureMeta, migrateLegacyMeta, rosterMacs, clampServiceH, calDefaults }` — Konsumenten: rasicross.js (Task 2), karts-page.js (Task 4/5).
  - `isDemoMac(mac) -> boolean` (Prefix `DE:MO:`)
  - `metaDefaults(idx) -> { name: 'Kart N', color: PALETTE[idx%5], lastSeenAt: null }`
  - `ensureMeta(map, mac, idx) -> { entry, created }` (mutiert map bei Neuanlage)
  - `migrateLegacyMeta(map, legacyJson) -> boolean` (true = mind. 1 Eintrag übernommen; wirft nie)
  - `rosterMacs(metaMap, registryMacs) -> string[]` (Registry-Reihenfolge zuerst, dann offline nach lastSeenAt absteigend)
  - `clampServiceH(v) -> number` (0..500, NaN→0)
  - `calDefaults() -> frisches Objekt` (identisch zu kart-registry makeKartState().calibration)

- [ ] **Step 1: Failing Tests schreiben** — `test/kart-roster.test.js` (neu, ESM wie die übrigen Tests):

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import RasiKartRoster from '../src/kart-roster.js';

const { isDemoMac, metaDefaults, ensureMeta, migrateLegacyMeta,
        rosterMacs, clampServiceH, calDefaults, PALETTE } = RasiKartRoster;

test('isDemoMac: DE:MO:-Prefix erkannt, echte MACs nicht', () => {
  assert.equal(isDemoMac('DE:MO:RA:SI:00:01'), true);
  assert.equal(isDemoMac('AA:BB:CC:DD:EE:01'), false);
  assert.equal(isDemoMac(null), false);
});

test('metaDefaults: Name nach Index, Farbe aus Palette (Modulo)', () => {
  assert.deepEqual(metaDefaults(0), { name: 'Kart 1', color: PALETTE[0], lastSeenAt: null });
  assert.equal(metaDefaults(5).color, PALETTE[0]);   // 5 % 5 = 0
  assert.equal(metaDefaults(6).name, 'Kart 7');
});

test('ensureMeta: legt Default an, laesst Bestehendes unangetastet', () => {
  const map = {};
  const r1 = ensureMeta(map, 'AA:01', 0);
  assert.equal(r1.created, true);
  assert.equal(map['AA:01'].name, 'Kart 1');
  map['AA:01'].name = 'Blitz';
  const r2 = ensureMeta(map, 'AA:01', 3);
  assert.equal(r2.created, false);
  assert.equal(r2.entry.name, 'Blitz');
});

test('migrateLegacyMeta: uebernimmt Name/Farbe, ergaenzt lastSeenAt', () => {
  const map = {};
  const legacy = JSON.stringify({ 'AA:01': { name: 'Rot', color: '#e85a7a' } });
  assert.equal(migrateLegacyMeta(map, legacy), true);
  assert.deepEqual(map['AA:01'], { name: 'Rot', color: '#e85a7a', lastSeenAt: null });
});

test('migrateLegacyMeta: nur wenn Ziel leer (idempotent)', () => {
  const map = { 'BB:02': { name: 'Neu', color: '#3aa0e8', lastSeenAt: 5 } };
  const legacy = JSON.stringify({ 'AA:01': { name: 'Alt', color: '#fff' } });
  assert.equal(migrateLegacyMeta(map, legacy), false);
  assert.equal(map['AA:01'], undefined);
});

test('migrateLegacyMeta: korruptes JSON / null wirft nicht', () => {
  assert.equal(migrateLegacyMeta({}, '{kaputt'), false);
  assert.equal(migrateLegacyMeta({}, null), false);
  assert.equal(migrateLegacyMeta({}, JSON.stringify({ 'AA:01': 'quatsch' })), false);
});

test('rosterMacs: Registry zuerst (Reihenfolge erhalten), offline nach lastSeenAt', () => {
  const meta = {
    'OF:ALT': { name: 'a', color: '#fff', lastSeenAt: 100 },
    'ON:01':  { name: 'b', color: '#fff', lastSeenAt: 999 },
    'OF:NEU': { name: 'c', color: '#fff', lastSeenAt: 200 },
  };
  assert.deepEqual(rosterMacs(meta, ['ON:01', 'DE:MO:RA:SI:00:01']),
    ['ON:01', 'DE:MO:RA:SI:00:01', 'OF:NEU', 'OF:ALT']);
  assert.deepEqual(rosterMacs({}, []), []);
});

test('clampServiceH: 0..500, NaN -> 0', () => {
  assert.equal(clampServiceH(10.5), 10.5);
  assert.equal(clampServiceH(-3), 0);
  assert.equal(clampServiceH(9999), 500);
  assert.equal(clampServiceH('abc'), 0);
});

test('calDefaults: frisches Objekt mit Registry-Defaults', () => {
  const a = calDefaults(); const b = calDefaults();
  assert.notEqual(a, b);
  assert.deepEqual(a, { gxZero: 0, gyZero: 0, swapG: false, invertGx: false,
                        invertGy: false, invertYaw: false, invertRollRate: false, rollZero: 0 });
});
```

- [ ] **Step 2: Fehlschlag verifizieren** — Run: `npm test 2>&1 | Select-String "^. (tests|pass|fail)"` → `fail` > 0 (Modul fehlt, ERR_MODULE_NOT_FOUND).

- [ ] **Step 3: Implementierung** — `src/kart-roster.js` (neu):

```js
// ============================================================
//  RasiCross — kart-roster.js  (pure Kart-Stammdaten-Logik, Phase 46)
// ============================================================
//  Roster-Mechanik ohne DOM/State: Meta-Defaults, Migration des alten
//  rasi.kartMeta.v1-Keys, Anzeige-Sortierung, Klemmen. Persistenz und
//  Maps besitzt rasicross.js (_persistedKarts.meta); hier nur Funktionen.
//  Laeuft unter node:test und im Browser. Wirft nie.
// ============================================================

  const PALETTE = ['#3aa0e8', '#e8a13a', '#5ad17a', '#e85a7a', '#b07ae8'];

  function isDemoMac(mac) { return String(mac || '').indexOf('DE:MO:') === 0; }

  function metaDefaults(idx) {
    const i = Math.max(0, Number(idx) || 0);
    return { name: 'Kart ' + (i + 1), color: PALETTE[i % PALETTE.length], lastSeenAt: null };
  }

  function ensureMeta(map, mac, idx) {
    if (!map[mac]) return { entry: (map[mac] = metaDefaults(idx)), created: true };
    return { entry: map[mac], created: false };
  }

  // Alt-Key rasi.kartMeta.v1 (kart-bar.js bis Phase 45): { mac: {name,color} }.
  // Nur in ein LEERES Ziel migrieren (idempotent); unbrauchbare Eintraege
  // ueberspringen; korruptes JSON -> false (Aufrufer laesst den Key stehen).
  function migrateLegacyMeta(map, legacyJson) {
    if (Object.keys(map).length || !legacyJson) return false;
    let legacy;
    try { legacy = JSON.parse(legacyJson); } catch (e) { return false; }
    if (!legacy || typeof legacy !== 'object') return false;
    let n = 0;
    for (const mac of Object.keys(legacy)) {
      const m = legacy[mac];
      if (!m || typeof m !== 'object' || typeof m.name !== 'string') continue;
      map[mac] = { name: m.name, color: typeof m.color === 'string' ? m.color : PALETTE[0],
                   lastSeenAt: null };
      n++;
    }
    return n > 0;
  }

  // Anzeige-Reihenfolge der Karts-Seite: Session-Karts (Registry-Reihenfolge,
  // inkl. Demo) zuerst, dahinter offline-Roster nach lastSeenAt absteigend.
  function rosterMacs(metaMap, registryMacs) {
    const online = registryMacs.slice();
    const offline = Object.keys(metaMap)
      .filter(m => online.indexOf(m) === -1)
      .sort((a, b) => (metaMap[b].lastSeenAt || 0) - (metaMap[a].lastSeenAt || 0));
    return online.concat(offline);
  }

  function clampServiceH(v) { return Math.max(0, Math.min(500, Number(v) || 0)); }

  // Bewusst dupliziert zu kart-registry.makeKartState().calibration —
  // kart-registry bleibt dependency-frei; bei Feldaenderungen BEIDE pflegen.
  function calDefaults() {
    return { gxZero: 0, gyZero: 0, swapG: false, invertGx: false,
             invertGy: false, invertYaw: false, invertRollRate: false, rollZero: 0 };
  }

  // ESM-Export: Default-Objekt (Konvention der Objekt-Module, Phase 42)
  export default { PALETTE, isDemoMac, metaDefaults, ensureMeta,
                   migrateLegacyMeta, rosterMacs, clampServiceH, calDefaults };
```

- [ ] **Step 4: Tests grün** — Run: `npm test 2>&1 | Select-String "^. (tests|pass|fail)"` → `tests 187`, `pass 187`, `fail 0` (178 + 9 neue). `npx eslint src/kart-roster.js test/kart-roster.test.js` → 0.

- [ ] **Step 5: Commit**

```bash
git add src/kart-roster.js test/kart-roster.test.js
git commit -m "feat(karts): kart-roster.js -- pure Roster-Logik (Defaults, Migration, Sortierung) mit Tests"
```

---

### Task 2: Roster-Integration in rasicross.js

**Files:**
- Modify: `src/rasicross.js` (Import-Block; `_persistedKarts` ~Z. 218; `rasiPersistForget` ~Z. 220; saveData-Payload ~Z. 266; loadData ~Z. 333; Paketpfad ~Z. 822; init ~Z. 1179; Export-Block am Dateiende)

**Interfaces:**
- Consumes: `RasiKartRoster` aus Task 1.
- Produces (neue rasicross-Exporte, Konsumenten Task 3-5/7): `kartMetaFor(mac, idx) -> {name,color,lastSeenAt}` · `updateKartMeta(mac, patch) -> entry` · `kartRosterMacs() -> string[]` · `kartCalFor(mac) -> obj|null` · `kartEngineFor(mac) -> obj|null`.

- [ ] **Step 1: Import** — im Import-Block (nach `import RasiKart3D …`):

```js
import RasiKartRoster from './kart-roster.js';
```

- [ ] **Step 2: Maps + Accessoren** — Region um `const _persistedKarts` frisch lesen; ersetzen/ergänzen zu:

```js
const _persistedKarts = { cal: {}, eng: {}, meta: {} };
// Demo-Karts (DE:MO:*) bekommen Session-Meta, nie Persistenz (Phase 46).
const _demoMeta = {};
function rasiPersistForget(mac) {
  delete _persistedKarts.cal[mac];
  delete _persistedKarts.eng[mac];
  delete _persistedKarts.meta[mac];
  delete _demoMeta[mac];
}
// Roster-Accessoren (Phase 46): einzige Meta-Quelle fuer Chips, Grid,
// Verbindungs-Detail und Karts-Seite. idx nur fuer die Default-Anlage.
function kartMetaFor(mac, idx) {
  const map = RasiKartRoster.isDemoMac(mac) ? _demoMeta : _persistedKarts.meta;
  const r = RasiKartRoster.ensureMeta(map, mac, idx);
  if (r.created && map === _persistedKarts.meta) saveDataDebounced();
  return r.entry;
}
function updateKartMeta(mac, patch) {
  const m = kartMetaFor(mac, Math.max(0, state.karts.macs().indexOf(mac)));
  Object.assign(m, patch);
  if (!RasiKartRoster.isDemoMac(mac)) saveDataDebounced();
  return m;
}
function kartRosterMacs() {
  return RasiKartRoster.rosterMacs(_persistedKarts.meta, state.karts.macs());
}
// Kalibrierung/Motor eines Karts — live-Bucket bevorzugt, sonst die
// persistierte Ablage (offline-Karts auf der Karts-Seite), sonst null.
function kartCalFor(mac) {
  if (state.karts.has(mac)) return state.karts.get(mac).calibration;
  return _persistedKarts.cal[mac] || null;
}
function kartEngineFor(mac) {
  if (state.karts.has(mac)) return state.karts.get(mac).engine;
  return _persistedKarts.eng[mac] || null;
}
```

- [ ] **Step 3: saveData-Payload** — im payload-Objekt nach `kartsCal: _kartsCal, kartsEngine: _kartsEngine,`:

```js
      kartsMeta: _persistedKarts.meta,
```

- [ ] **Step 4: loadData + Legacy-Migration** — in `loadData()` nach `Object.assign(_persistedKarts.eng, _eng);`:

```js
    if (d.kartsMeta && typeof d.kartsMeta === 'object') Object.assign(_persistedKarts.meta, d.kartsMeta);
```

Direkt nach `loadData` eine neue Funktion (und Aufruf in `init()` DIREKT NACH `loadData();`):

```js
// Phase 46: Alt-Key des Chip-Editors einmalig ins Roster uebernehmen.
function migrateLegacyKartMeta() {
  try {
    const legacy = localStorage.getItem('rasi.kartMeta.v1');
    if (RasiKartRoster.migrateLegacyMeta(_persistedKarts.meta, legacy)) {
      localStorage.removeItem('rasi.kartMeta.v1');
      saveDataDebounced();
    }
  } catch (e) { /* Migration darf den Start nie verhindern */ }
}
```

- [ ] **Step 5: lastSeenAt im Paketpfad** — Region `k.connection.lastPacketAt = Date.now();` frisch lesen; danach einfügen:

```js
    kartMetaFor(_mac, Math.max(0, state.karts.macs().indexOf(_mac))).lastSeenAt = Date.now();
```

- [ ] **Step 6: Export-Block erweitern** — am Dateiende ergänzen: `kartMetaFor, updateKartMeta, kartRosterMacs, kartCalFor, kartEngineFor,`.

- [ ] **Step 7: Gates** — `npx eslint src/rasicross.js` → 0; `npm test` → 187/187 (rasicross wird nicht von Tests importiert); `npx vite build 2>&1 | Select-Object -Last 3` → baut.

- [ ] **Step 8: Commit** — `git add src/rasicross.js && git commit -m "feat(karts): Kart-Roster in rasicross -- kartsMeta persistiert, Accessoren, Legacy-Migration, lastSeenAt"`

---

### Task 3: kart-bar.js delegiert an das Roster

**Files:**
- Modify: `src/kart-bar.js` (komplette Datei frisch lesen — 194 Zeilen)

**Interfaces:**
- Consumes: `kartMetaFor` aus Task 2.
- Produces: `export default { render, metaFor: metaForState }` — `openEditor`/`forgetKart` entfallen (forgetKart kommt in Task 5 nach karts-page.js). `metaForState(state, mac, idx)`-Signatur bleibt für pit-wall/live-ui/kart-overview.

- [ ] **Step 1: Kopf + Meta-Layer ersetzen** — Imports werden zu:

```js
import { kartMetaFor } from './rasicross.js';
import { setLiveView } from './live-ui.js';
```

`LS_KEY`, `PALETTE`, `loadMeta`, `saveMeta`, `_metaDirty`, `metaFor`, `saveMetaIfDirty` ersatzlos löschen. `metaForState` wird zu:

```js
  // Signatur-Wrapper (state wird seit Phase 46 ignoriert): pit-wall.js,
  // live-ui.js und kart-overview.js rufen metaFor(state, mac, idx).
  function metaForState(state, mac, idx) {
    return kartMetaFor(mac, idx);
  }
```

- [ ] **Step 2: render() umstellen** — `const meta = state.kartMeta …`-Zeilen und `saveMetaIfDirty(meta)` löschen; `const m = metaFor(meta, mac, i);` → `const m = kartMetaFor(mac, i);`. Den ✏-Button aus dem Chip-HTML entfernen (nur noch `kart-chip-main`-Button), den `.kart-edit`-onclick-Block löschen, im Focus-Restore die `_feEdit`-Variante entfernen (nur noch `kart-chip-main`-Selektor).

- [ ] **Step 3: Editor + Forget löschen** — `_onDocClick`, `closeEditor`, `_onEditKey`, `openEditor`, `forgetKart` komplett entfernen. Export: `export default { render, metaFor: metaForState };`

- [ ] **Step 4: Gates** — `npx eslint src/kart-bar.js` → 0 (no-unused-vars fängt vergessene Imports); `npm test` 187; `npx vite build` ok. Hinweis: `escHtml` bleibt (render nutzt es).

- [ ] **Step 5: Commit** — `git add src/kart-bar.js && git commit -m "refactor(karts): Chip-Leiste liest Roster-Accessor, Editor-Popover-Logik und Pinselknopf entfernt"`

---

### Task 4: Karts-Tab-Gerüst (Nav, Section, CSS, Render, Verdrahtung)

**Files:**
- Create: `src/karts-page.js` (Render-Teil; Aktionen in Task 5)
- Modify: `index.html` (Nav ~Z. 2441; neue Section nach `</section>` von tab-drivers ~Z. 3142; CSS nach `.driver-stat-card`-Block ~Z. 1745), `src/app.js`, `src/rasicross.js` (setupTabs), `src/live-ui.js` (1-Hz-Hook)

**Interfaces:**
- Consumes: `kartMetaFor`, `kartRosterMacs`, `kartEngineFor`, `kartCalFor` (Task 2); `RasiEngine` (hoursText, sinceServiceMs, serviceDue); `RasiKartRoster` (isDemoMac, PALETTE).
- Produces: `export { renderKartsTab }` — Konsumenten: rasicross (Tab-Hook), live-ui (1-Hz), app.js (Import-Kette).

- [ ] **Step 1: Nav-Item** — in `index.html` nach dem Fahrer-Button (`data-tab="drivers"`-Block, endet `</button>`) einfügen:

```html
    <button class="nav-item" data-tab="karts">
      <span class="nav-icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 3v6M12 15v6M3 12h6M15 12h6"/><circle cx="12" cy="12" r="2.5"/></svg></span>
      Karts
    </button>
```

- [ ] **Step 2: Tab-Section** — nach `</section>` des tab-drivers-Blocks (vor dem Kommentar `TAB: VERBINDUNG`):

```html
<!-- ============================================================
     TAB: KARTS (Phase 46)
     ============================================================ -->
<section class="tab" id="tab-karts">
  <div class="pw-library">
    <div class="pw-lib-head pw-lib-head-row">
      <div class="pw-lib-title">
        <span class="pw-lib-bar"></span>
        <span>Karts</span>
        <span class="pw-lib-count"><span id="kartCount">0</span> · Karts</span>
      </div>
      <button class="btn ghost" id="kartsResetAllBtn">Alle Karts zurücksetzen</button>
    </div>
    <div id="kartCardsList"><div class="muted">Noch keine Karts — sie erscheinen mit dem ersten Telemetrie-Paket.</div></div>
  </div>
</section>
```

- [ ] **Step 3: CSS** — nach dem `.dstat b{…}`-Block (Region frisch lesen) einfügen:

```css
/* ── Karts-Tab (Phase 46): Karte pro Kart, Stil wie driver-stat-card ── */
.kart-card{padding:16px;background:var(--surf);border:1px solid var(--bor);
  border-radius:var(--r-xl);border-left:4px solid var(--kart,var(--pr));
  margin-bottom:10px;transition:var(--t)}
.kart-card.active{box-shadow:0 0 0 1px var(--kart,var(--pr)) inset}
.kart-card.offline{opacity:.72}
.kc-head{display:grid;grid-template-columns:auto 1fr auto;gap:12px;align-items:center;margin-bottom:10px}
.kc-dot{width:14px;height:14px;border-radius:50%;background:var(--kart,var(--pr))}
.kc-name-input{font-family:var(--sans);font-size:16px;font-weight:700;color:var(--tx);
  background:transparent;border:1px solid transparent;border-radius:var(--r-md);padding:4px 8px;max-width:220px}
.kc-name-input:hover,.kc-name-input:focus{border-color:var(--bor);background:var(--soft)}
.kc-mac{font-family:var(--mono);font-size:10.5px;color:var(--mut);font-weight:600;margin-top:2px}
.kc-badge{font-family:var(--mono);font-size:9px;font-weight:700;letter-spacing:.08em;
  padding:2px 7px;border-radius:999px;border:1px solid var(--bor);color:var(--mut);margin-left:8px}
.kc-badge.demo{color:var(--pr);border-color:var(--pr)}
.kc-live{font-family:var(--mono);font-size:11px;color:var(--mut);margin-bottom:10px;display:flex;gap:12px;flex-wrap:wrap}
.kc-live .rec{color:#e85a7a;font-weight:700}
.kc-swatches{display:flex;gap:6px}
.kc-sw{width:18px;height:18px;border-radius:50%;cursor:pointer;border:2px solid transparent}
.kc-sw.active{border-color:var(--tx)}
.kc-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(130px,1fr));gap:8px;margin-bottom:10px}
.kc-actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.kc-actions .grow{flex:1}
.kc-interval{width:70px;text-align:right}
.kc-warn{font-family:var(--mono);font-size:11px;color:#e8a13a;font-weight:700}
```

- [ ] **Step 4: `src/karts-page.js` (Render-Teil)** — neu:

```js
// ============================================================
//  RasiCross — karts-page.js  (Karts-Tab, Phase 46)
// ============================================================
//  Eine Karte pro Roster-Kart (persistent) + Session-/Demo-Karts.
//  Rendert aus kartRosterMacs/kartMetaFor + Registry-Buckets; die
//  Aktionen (Umbenennen, Farbe, Vergessen, Service, Kalibrierung,
//  Reset-alle) haengen direkt an den Karten-Elementen.
//  Nur Deklarationen auf Top-Level — kein Code laeuft beim Laden.
// ============================================================
import { state, $, esc, setText, rcConfirm, rcToast, saveData, saveDataDebounced,
         kartMetaFor, updateKartMeta, kartRosterMacs, kartCalFor, kartEngineFor,
         rasiPersistForget } from './rasicross.js';
import RasiEngine from './engine.js';
import RasiKartRoster from './kart-roster.js';
import RasiKartBar from './kart-bar.js';

function _liveHtml(k, now) {
  const age = k.connection.lastPacketAt ? (now - k.connection.lastPacketAt) : 99999;
  const ageStr = age < 99999 ? (age / 1000).toFixed(1) + 's' : '--';
  const hzMap = state._kartHz || {};
  const hz = hzMap[k.connection.kartMac] != null ? hzMap[k.connection.kartMac] : '--';
  const rssi = k.connection.rssi != null ? k.connection.rssi + 'dBm' : '--';
  const batt = (k.batt && k.batt.present) ? ((k.batt.soc | 0) + '%') : '--';
  const rec = k.recording.armed ? '<span class="rec">●REC</span>' : '';
  return '<div class="kc-live"><span>' + hz + 'Hz</span><span>' + rssi + '</span>'
    + '<span>Alter ' + ageStr + '</span><span>Akku ' + batt + '</span>' + rec + '</div>';
}

function _engineHtml(mac) {
  const e = kartEngineFor(mac);
  if (!e) return '';
  const due = RasiEngine.serviceDue(e.totalMs, e.lastServiceMs, e.serviceIntervalH);
  return '<div class="kc-grid">'
    + '<div class="dstat"><span>Motorlaufzeit</span><b>' + RasiEngine.hoursText(e.totalMs) + '</b></div>'
    + '<div class="dstat"><span>Seit Wartung</span><b>' + RasiEngine.hoursText(RasiEngine.sinceServiceMs(e.totalMs, e.lastServiceMs)) + '</b></div>'
    + '</div>'
    + '<div class="kc-actions">'
    + (due ? '<span class="kc-warn">🔧 Wartung fällig</span>' : '')
    + '<label class="kc-mac">Intervall (h) <input type="number" class="kc-interval" data-action="interval" data-mac="' + esc(mac) + '" value="' + e.serviceIntervalH + '" min="0" max="500" step="0.5"></label>'
    + '<button type="button" class="btn ghost" data-action="service" data-mac="' + esc(mac) + '">Wartung erledigt</button>'
    + '</div>';
}

function _calHtml(mac) {
  const c = kartCalFor(mac);
  if (!c) return '';
  const flags = [c.swapG && 'GxGy-Swap', c.invertGx && 'Gx-Inv', c.invertGy && 'Gy-Inv',
                 c.invertYaw && 'Yaw-Inv', c.invertRollRate && 'Roll-Inv'].filter(Boolean).join(' · ') || 'keine Flags';
  return '<div class="kc-grid">'
    + '<div class="dstat"><span>Gx/Gy-Offset</span><b>' + (Number(c.gxZero) || 0).toFixed(2) + ' / ' + (Number(c.gyZero) || 0).toFixed(2) + '</b></div>'
    + '<div class="dstat"><span>Roll-Null</span><b>' + (Number(c.rollZero) || 0).toFixed(1) + '°</b></div>'
    + '<div class="dstat"><span>Achsen</span><b style="font-size:11px">' + flags + '</b></div>'
    + '</div>'
    + '<div class="kc-actions"><button type="button" class="btn ghost" data-action="calreset" data-mac="' + esc(mac) + '">Kalibrierung zurücksetzen</button>'
    + '<span class="grow"></span>'
    + '<button type="button" class="btn danger" data-action="forget" data-mac="' + esc(mac) + '">Kart vergessen</button></div>';
}

function _cardHtml(mac, idx, now) {
  const m = kartMetaFor(mac, idx);
  const online = state.karts.has(mac);
  const k = online ? state.karts.get(mac) : null;
  const demo = RasiKartRoster.isDemoMac(mac);
  const activeCls = (mac === state.activeKartMac && online) ? ' active' : '';
  const offCls = online ? '' : ' offline';
  const badge = demo ? '<span class="kc-badge demo">DEMO</span>'
    : (online ? '' : '<span class="kc-badge">OFFLINE</span>');
  const seen = (!online && m.lastSeenAt)
    ? '<div class="kc-live"><span>Zuletzt gesehen ' + new Date(m.lastSeenAt).toLocaleString('de-DE') + '</span></div>'
    : (!online ? '<div class="kc-live"><span>Noch nie verbunden</span></div>' : '');
  const swatches = RasiKartRoster.PALETTE.map(col =>
    '<span class="kc-sw' + (col === m.color ? ' active' : '') + '" data-action="color" data-mac="' + esc(mac) + '" data-color="' + col + '" style="background:' + col + '"></span>').join('');
  return '<div class="kart-card' + activeCls + offCls + '" data-mac="' + esc(mac) + '" style="--kart:' + esc(m.color) + '">'
    + '<div class="kc-head">'
    +   '<span class="kc-dot"></span>'
    +   '<div><input type="text" class="kc-name-input" data-action="name" data-mac="' + esc(mac) + '" maxlength="20" value="' + esc(m.name) + '">' + badge
    +   '<div class="kc-mac">' + esc(mac) + '</div></div>'
    +   '<div class="kc-swatches">' + swatches + '</div>'
    + '</div>'
    + (online ? _liveHtml(k, now) : seen)
    + _engineHtml(mac) + _calHtml(mac)
    + '</div>';
}

function renderKartsTab() {
  const list = $('kartCardsList');
  if (!list) return;
  // Tipp-Schutz: waehrend ein Karten-Input den Fokus hat, nicht neu bauen
  // (der 1-Hz-Refresh wuerde sonst die Eingabe verwerfen).
  const ae = document.activeElement;
  if (ae && list.contains(ae) && ae.tagName === 'INPUT') return;
  const macs = kartRosterMacs();
  setText('kartCount', macs.length);
  if (!macs.length) {
    list.innerHTML = '<div class="muted">Noch keine Karts — sie erscheinen mit dem ersten Telemetrie-Paket.</div>';
    return;
  }
  const now = Date.now();
  list.innerHTML = macs.map((mac, i) => _cardHtml(mac, i, now)).join('');
  bindCardEvents(list);
}

// Task 5 fuellt die Aktionen; das Geruest verdrahtet nur die Aktiv-Wahl.
function bindCardEvents(list) {
  list.querySelectorAll('.kart-card').forEach(card => {
    card.addEventListener('click', (ev) => {
      if (ev.target.closest('[data-action]')) return;   // Inputs/Buttons nicht kapern
      const mac = card.getAttribute('data-mac');
      if (state.karts.has(mac) && state.karts.setActive(mac)) {
        state.activeKartMac = mac;
        RasiKartBar.render(state);
        renderKartsTab();
      }
    });
  });
}

// ESM-Export (Phase 46)
export { renderKartsTab };
```

(Hinweis für den Umsetzenden: `rcConfirm`, `rcToast`, `saveData`, `saveDataDebounced`, `updateKartMeta`, `rasiPersistForget` werden erst in Task 5 benutzt — bis dahin meldet ESLint sie als unused. Entweder die Imports erst in Task 5 ergänzen oder einen `void [...]`-Marker setzen; Ersteres ist sauberer: In DIESEM Task nur `state, $, esc, setText, kartMetaFor, kartRosterMacs, kartCalFor, kartEngineFor` + die drei Modul-Defaults importieren.)

- [ ] **Step 5: Verdrahtung** — `src/app.js`: `import './karts-page.js';` nach `import './kart-overview.js';`. `src/rasicross.js` `setupTabs()`: nach `if (tab === 'drivers') renderDrivers();` einfügen `if (tab === 'karts') renderKartsTab();` + Import `import { renderKartsTab } from './karts-page.js';`. `src/live-ui.js`: im 1000-ms-Intervall von `initLiveUiLoops()` (Region mit `footerKm` frisch lesen) ergänzen:

```js
  // Karts-Tab: Live-Werte der Karten im 1-Hz-Takt (nur bei aktivem Tab).
  if (document.body.dataset.tab === 'karts') { try { renderKartsTab(); } catch (e) {} }
```

+ Import `import { renderKartsTab } from './karts-page.js';` (Zyklus live-ui→karts-page→kart-bar→live-ui ist deklarationsrein — zulässig).

- [ ] **Step 6: Gates** — `npx eslint src/ index.html 2>$null; npx eslint src/` → 0; `npm test` 187; `npm run test:e2e` → 6 passed (Karts-Tab existiert, wird von den Alt-Specs nicht besucht; app.spec „alle Tabs rendern" iteriert über die TABS-Konstante — sie enthält `karts` noch nicht, Erweiterung kommt in Task 7). Sichtprüfung: `npm start`, Karts-Tab öffnen, Demo starten → 3 Karten.

- [ ] **Step 7: Commit** — `git add index.html src/karts-page.js src/app.js src/rasicross.js src/live-ui.js && git commit -m "feat(karts): Karts-Tab -- Nav, Section, Karten-Rendering, 1-Hz-Refresh, Aktiv-Wahl"`

---

### Task 5: Karten-Aktionen (Rename, Farbe, Vergessen, Service, Kalibrierung, Reset-alle)

**Files:**
- Modify: `src/karts-page.js`, `src/pit-wall.js` (nur falls resetKarts dort Helfer braucht — nein: Umzug erfolgt als Kopie, Original fällt in Task 6)

**Interfaces:**
- Consumes: Task-2-Accessoren; `RasiKartRoster.clampServiceH/calDefaults`.
- Produces: vollständige `bindCardEvents`; `forgetKart(mac)` und `resetAllKarts()` modulintern (kein Export nötig — Buttons hängen dran).

- [ ] **Step 1: Imports ergänzen** — `rcConfirm, rcToast, saveData, saveDataDebounced, updateKartMeta, rasiPersistForget` in den rasicross-Import aufnehmen.

- [ ] **Step 2: Aktionen implementieren** — `bindCardEvents` ersetzen durch:

```js
function forgetKart(mac) {
  state.karts.forget(mac);
  // Bewusstes Vergessen loescht auch Kalibrierung/Motorstunden/Meta der MAC
  // (Semantik von Phase 39, jetzt eine Stelle: rasiPersistForget).
  rasiPersistForget(mac);
  if (state._kartHz) delete state._kartHz[mac];
  if (state.serial && state.serial.connected && window.rasiSerial && window.rasiSerial.writeLine) {
    try { window.rasiSerial.writeLine(JSON.stringify({ type: 'forget_kart_mac', mac })); } catch (e) {}
  }
  state.activeKartMac = state.karts.activeMac();
  saveData();
  rcToast('Kart vergessen');
  RasiKartBar.render(state);
  renderKartsTab();
}

async function resetAllKarts() {
  if (!await rcConfirm('Alle bekannten Karts vergessen? Namen/Farben, Kalibrierung und Motorstunden bleiben erhalten.',
      'Karts zurücksetzen', 'Zurücksetzen', true)) return;
  state.karts.reset();
  state._kartHz = {};
  state.activeKartMac = null;
  if (state.serial && state.serial.connected && window.rasiSerial && window.rasiSerial.writeLine) {
    try { window.rasiSerial.writeLine(JSON.stringify({ type: 'reset_karts' })); } catch (e) {}
  }
  rcToast('Alle Karts zurückgesetzt');
  RasiKartBar.render(state);
  renderKartsTab();
}

function bindCardEvents(list) {
  list.querySelectorAll('.kart-card').forEach(card => {
    card.addEventListener('click', (ev) => {
      if (ev.target.closest('[data-action]')) return;
      const mac = card.getAttribute('data-mac');
      if (state.karts.has(mac) && state.karts.setActive(mac)) {
        state.activeKartMac = mac;
        RasiKartBar.render(state);
        renderKartsTab();
      }
    });
  });
  list.querySelectorAll('[data-action="name"]').forEach(inp => {
    inp.oninput = () => {
      const mac = inp.getAttribute('data-mac');
      const idx = Math.max(0, state.karts.macs().indexOf(mac));
      updateKartMeta(mac, { name: inp.value.trim() || ('Kart ' + (idx + 1)) });
      RasiKartBar.render(state);
    };
    // Nach dem Verlassen des Felds einmal neu bauen (Tipp-Schutz-Ende).
    inp.onblur = () => renderKartsTab();
  });
  list.querySelectorAll('[data-action="color"]').forEach(sw => {
    sw.onclick = () => {
      updateKartMeta(sw.getAttribute('data-mac'), { color: sw.getAttribute('data-color') });
      RasiKartBar.render(state);
      renderKartsTab();
    };
  });
  list.querySelectorAll('[data-action="interval"]').forEach(inp => {
    inp.onchange = () => {
      const e = kartEngineFor(inp.getAttribute('data-mac'));
      if (!e) return;
      e.serviceIntervalH = RasiKartRoster.clampServiceH(inp.value);
      inp.value = e.serviceIntervalH;
      saveDataDebounced();
    };
    inp.onblur = () => renderKartsTab();
  });
  list.querySelectorAll('[data-action="service"]').forEach(btn => {
    btn.onclick = async () => {
      if (!await rcConfirm('Wartungszähler zurücksetzen? Seit-letzter-Wartung beginnt wieder bei 0.', 'Wartung', 'Zurücksetzen')) return;
      const e = kartEngineFor(btn.getAttribute('data-mac'));
      if (!e) return;
      e.lastServiceMs = e.totalMs;
      if ('_warned' in e) e._warned = false;
      saveData();
      rcToast('🔧 Wartung vermerkt');
      renderKartsTab();
    };
  });
  list.querySelectorAll('[data-action="calreset"]').forEach(btn => {
    btn.onclick = async () => {
      if (!await rcConfirm('Kalibrierung dieses Karts auf Werkswerte zurücksetzen?', 'Kalibrierung', 'Zurücksetzen', true)) return;
      const c = kartCalFor(btn.getAttribute('data-mac'));
      if (!c) return;
      Object.assign(c, RasiKartRoster.calDefaults());
      saveData();
      rcToast('Kalibrierung zurückgesetzt');
      renderKartsTab();
    };
  });
  list.querySelectorAll('[data-action="forget"]').forEach(btn => {
    btn.onclick = async () => {
      const mac = btn.getAttribute('data-mac');
      if (!await rcConfirm('Dieses Kart endgültig vergessen? Name, Farbe, Kalibrierung und Motorstunden werden gelöscht.', 'Kart vergessen', 'Vergessen', true)) return;
      forgetKart(mac);
    };
  });
}
```

Zusätzlich einmalige Kopf-Verdrahtung in `renderKartsTab()` (vor dem `macs`-Aufbau):

```js
  const resetBtn = $('kartsResetAllBtn');
  if (resetBtn && !resetBtn._bound) { resetBtn._bound = true; resetBtn.onclick = resetAllKarts; }
```

Hinweis: `forgetKart` fragt hier per rcConfirm (das alte ✏-Popover löschte ohne Rückfrage — bewusste kleine Verbesserung, im PR erwähnen).

- [ ] **Step 3: Gates** — `npx eslint src/karts-page.js` → 0; `npm test` 187; Sichtprüfung `npm start`: Demo starten → umbenennen (Chip folgt), Farbe wechseln, Service-Intervall ändern, Kart vergessen (Karte verschwindet, Chip-Leiste schrumpft).

- [ ] **Step 4: Commit** — `git add src/karts-page.js && git commit -m "feat(karts): Karten-Aktionen -- Rename/Farbe/Vergessen/Service/Kalibrier-Reset/Reset-alle"`

---

### Task 6: Altstellen entfernen (Popover, Verbindungs-Liste, Settings-Zeilen)

**Files:**
- Modify: `index.html` (4 Regionen — jede frisch lesen), `src/pit-wall.js`, `src/rasicross.js`, `src/live-ui.js`, `src/settings.js`

- [ ] **Step 1: index.html** —
  1. `#kartEditPopover`-DIV am Body-Ende (Region um `<div id="kartEditPopover" class="hidden">` … `</div>` vor dem Modul-Script) komplett löschen.
  2. Zugehörige CSS-Regeln löschen (Region um `#kartEditPopover{` frisch lesen — alle `#kartEditPopover`-Selektoren inkl. `.ke-row`/`label`-Untermengen; auch `#kartEditSwatches`-/`.sw`-Regeln, sofern sie NUR vom Popover genutzt werden — mit Grep `class="sw` gegen Restnutzung prüfen).
  3. Verbindungs-Tab: `<div id="connKartList"></div>` + `<button … id="resetKartsBtn">…</button>` (Region um Z. 3192) und die CSS-Regel `#connKartList{…}` löschen. `#connDetailTitle` bleibt.
  4. Settings: die drei `settings-row`-Blöcke Motorlaufzeit / Wartungsintervall / Seit letzter Wartung UND die `serviceDoneBtn`-Zeile (Region um Z. 3494–3506) löschen.

- [ ] **Step 2: pit-wall.js** — `renderConnKartList()` und `resetKarts()` komplett löschen; den Aufruf `renderConnKartList();` in `renderConnectionTab()` (Region frisch lesen) entfernen; `renderConnKartList, resetKarts` aus `void [...]`-Marker und Export-Block streichen. `RasiKartBar`-Import bleibt (connDetailTitle nutzt `metaFor`).

- [ ] **Step 3: rasicross.js** — `updateEngineUi()` löschen (Fn + Export-Eintrag); in `loadSettingsToUi()` die Zeilen `if ($('setServiceIntervalH')) …` und `updateEngineUi();` entfernen; in `applySettings`-Region die Zeile `state.engine.serviceIntervalH = …` entfernen; den `$('serviceDoneBtn').onclick`-Block in `init()` (Region um Z. 1384) komplett entfernen.

- [ ] **Step 4: live-ui.js** — `updateEngineUi` aus dem rasicross-Import und den Aufruf `updateEngineUi();` in `updateLiveUi()` (Kommentar „Motorlaufzeit-Anzeige…" mit-löschen) entfernen.

- [ ] **Step 5: settings.js** — die vier SETTINGS_INDEX-Einträge `engineHoursText`, `setServiceIntervalH`, `engineSinceServiceText`, `serviceDoneBtn` löschen (settings.test.js pinnt keine Zeilenzahl — nur nicht-leer + Feldform).

- [ ] **Step 6: Gates** — `npm run lint` → 0 (fängt jeden toten Bezug); `npm test` → 187/187; `npm run test:e2e` → 6 passed (app.spec „alle Tabs rendern" prüft u. a. tab-connection und tab-settings auf Inhalt — beide haben weiter Inhalt). Grep-Verifikation: `kartEditPopover|connKartList|resetKartsBtn|updateEngineUi|serviceDoneBtn|engineSinceServiceText` → 0 Treffer in src/ + index.html.

- [ ] **Step 7: Commit** — `git add index.html src/pit-wall.js src/rasicross.js src/live-ui.js src/settings.js && git commit -m "refactor(karts): Altstellen entfernt -- Chip-Popover, Verbindungs-Kartliste, Settings-Motor-Zeilen"`

---

### Task 7: RasiTest-Erweiterung + e2e/karts.spec.js (Smoke-Gate)

**Files:**
- Modify: `src/app.js`, `e2e/app.spec.js` (TABS-Liste)
- Create: `e2e/karts.spec.js`

**Interfaces:**
- Consumes: `window.RasiTest` (Phase 42), `updateKartMeta`/`kartRosterMacs` (Task 2).
- Produces: RasiTest um `updateKartMeta, kartRosterMacs` erweitert.

- [ ] **Step 1: app.js** — Import-Zeile erweitern und Brücke ergänzen:

```js
import { state, saveData, activeKart, armRecording,
         updateKartMeta, kartRosterMacs } from './rasicross.js';
```

und im `window.RasiTest`-Objekt: `updateKartMeta, kartRosterMacs,` ergänzen.

- [ ] **Step 2: app.spec.js** — TABS-Konstante (frisch lesen) um `'karts'` ergänzen: `const TABS = ['live', 'detail', 'races', 'drivers', 'karts', 'track', 'connection', 'settings'];` — Achtung: der Inhalts-Check verlangt nicht-leeres innerHTML; `#tab-karts` enthält statisch die pw-library → erfüllt.

- [ ] **Step 3: `e2e/karts.spec.js`** (neu):

```js
'use strict';
// Smoke: Karts-Tab (Phase 46) — Demo-Karten, Rename->Chip-Leiste,
// Aktiv-Wahl per Karten-Klick, Offline-Persistenz nach Reload.
const { test, expect } = require('@playwright/test');
const { launchApp, closeApp } = require('./helpers');

let app, page, errors, userData;

test.beforeEach(async () => {
  ({ app, page, errors, userData } = await launchApp());
});

test.afterEach(async () => {
  await closeApp(app, userData);
});

async function startDemo() {
  await page.click('.nav-item[data-tab="connection"]');
  await page.click('#modeDemoBtn');
  await page.click('#demoStartBtn');
  await page.waitForFunction(() => RasiTest.state.demo.running === true);
}

test('Karts-Tab zeigt 3 Demo-Karten, Rename wirkt in der Chip-Leiste', async () => {
  await startDemo();
  await page.click('.nav-item[data-tab="karts"]');
  await page.waitForFunction(() => document.querySelectorAll('#kartCardsList .kart-card').length === 3);
  expect(await page.locator('#kartCardsList .kc-badge.demo').count()).toBe(3);
  const nameInput = page.locator('#kartCardsList .kc-name-input').first();
  await nameInput.fill('Blitz');
  await nameInput.dispatchEvent('input');
  await page.click('.nav-item[data-tab="live"]');
  await page.waitForFunction(() =>
    document.querySelector('#kartBar') && document.querySelector('#kartBar').textContent.includes('Blitz'));
  expect(errors).toEqual([]);
});

test('Karten-Klick wechselt das aktive Kart', async () => {
  await startDemo();
  await page.click('.nav-item[data-tab="karts"]');
  await page.waitForFunction(() => document.querySelectorAll('#kartCardsList .kart-card').length === 3);
  const secondMac = await page.evaluate(() => RasiTest.state.karts.macs()[1]);
  await page.click('#kartCardsList .kart-card[data-mac="' + secondMac + '"] .kc-mac');
  await page.waitForFunction((mac) => RasiTest.state.activeKartMac === mac, secondMac);
  expect(errors).toEqual([]);
});

test('Persistentes Kart erscheint offline nach Reload', async () => {
  await page.evaluate(() => {
    RasiTest.updateKartMeta('AA:BB:CC:DD:EE:01',
      { name: 'Scheunenkart', color: '#5ad17a', lastSeenAt: Date.now() - 3600000 });
    window.saveData();
  });
  await page.reload();
  await page.waitForFunction(() =>
    !!(window.RasiTest && window.RasiTest.state && window.RasiTest.state.karts));
  await page.click('.nav-item[data-tab="karts"]');
  await page.waitForFunction(() =>
    document.querySelectorAll('#kartCardsList .kart-card.offline').length >= 1);
  const txt = await page.locator('#kartCardsList .kart-card.offline').first().textContent();
  expect(txt).toContain('Scheunenkart');
  expect(errors).toEqual([]);
});
```

- [ ] **Step 4: Smoke-Gate** — Run: `npm run test:e2e 2>&1 | Select-Object -Last 8` → Expected: **9 passed** (2 app + 3 demo + 1 replay + 3 karts). Bei Timeout im Rename-Test: prüfen, ob der 1-Hz-Rebuild das Input klaut (Tipp-Schutz greift nur bei Fokus — `fill` fokussiert; ggf. `await nameInput.focus()` vor `fill`).

- [ ] **Step 5:** `npm test` (187) + Commit: `git add src/app.js e2e && git commit -m "test(karts): Smoke fuer Karts-Tab -- Demo-Karten, Rename, Aktiv-Wahl, Offline-Persistenz"`

---

### Task 8: Finale Gates, EXE, graphify, Plan-Doc, Push + PR

- [ ] **Step 1: Komplettes Rezept**
  - `npm test` → 187/187, `npm run lint` → 0, `npm run test:e2e` → 9 passed
  - `node --check main.js`, `preload.js`, `tiles.js` → 0
  - Python unverändert: `python -m py_compile sender.py bridge.py esp_libs/*.py`; `python -m unittest discover -s test -p "test_*.py"` → 56 OK
  - Grep-Schlussprüfung: `rasi.kartMeta.v1` nur noch in kart-roster.js (Migrations-Kommentar) + rasicross.js (migrateLegacyKartMeta)
- [ ] **Step 2: Portable-EXE** — `npm run build-portable` → `release/RasiCross-Telemetry-Portable.exe`; Sichtprüfung: Karts-Tab, Demo-Karten, Rename, Offline-Karte nach Neustart der EXE. **Bestehende SAVE_KEY-Daten + Chip-Namen aus v1.0.7 (Alt-Key-Migration) = User-Abnahme.**
- [ ] **Step 3:** `__pycache__` löschen; `git status` sauber; `graphify update .`; Plan-Doc committen (`docs(plan): Phase 46 Karts-Tab Implementierungsplan`); Push; PR gegen `main` (Basis rebased nach #66-Merge automatisch) mit Gate-Nachweisen + Pflicht-Fußzeile.

---

## Hardware/Manual Acceptance Checklist (User)

- [ ] GitHub Actions: js/python/smoke/Build grün auf dem PR.
- [ ] Portable-EXE: Karts-Tab zeigt bestehende Karts mit den ALTEN Chip-Namen/-Farben (Migration `rasi.kartMeta.v1`).
- [ ] Motorstunden/Service-Werte stimmen mit dem Stand vor dem Update überein.
- [ ] Echte Hardware: Kart verbinden → Karte wird „online", Hz/RSSI plausibel; Kart aus → nach Neustart als Offline-Karte mit „zuletzt gesehen".

## Self-Review

- **Spec-Coverage:** Tab+Karte (UI-Abschnitt) → Tasks 4/5; Roster+Migration → Tasks 1/2; Altstellen entfallen → Tasks 3/6; Demo-Badge/nie-persistiert → Locked Decision + Task 1 (isDemoMac) + Task 2 (_demoMeta); Sortierung → Task 1 (rosterMacs); Tests → Tasks 1/7; Nicht-Ziele (Registry/Fassade/Protokoll) → keine Task berührt sie.
- **Bekannte Risiken verankert:** 1-Hz-Rebuild vs. Tippen (Tipp-Schutz + onblur-Rebuild), Offline-Aktionen ohne Registry-Bucket (kartCalFor/kartEngineFor), ESM-Zyklus live-ui↔karts-page (deklarationsrein), settings.test (pinnt keine Zeilen), app.spec-TABS-Erweiterung (Task 7 Step 2), Legacy-Key-Migration auch ohne SAVE_KEY (init-Aufruf nach loadData).
- **Typ-Konsistenz:** `kartMetaFor(mac, idx)`/`updateKartMeta(mac, patch)`/`kartRosterMacs()`/`kartCalFor(mac)`/`kartEngineFor(mac)` identisch in Tasks 2/4/5/7; RasiKartBar.metaFor-Signatur `(state, mac, idx)` unverändert (Task 3) für pit-wall/live-ui/kart-overview.
- **Platzhalter-Scan:** alle Code-Schritte tragen vollständigen Code; Regionen mit Zeilendrift sind als „frisch lesen" markiert (CRLF-Konvention), nicht als TBD.

## Phase Map

- Phase 42: Vite + ESM — PR #66 (Voraussetzung, Branch gestapelt).
- Phasen 43–45 (State-Redesign, rasicross-Zerlegung, Firmware): unabhängig; 43 profitiert davon, dass karts-page nur `state.karts.get(mac)` liest.
- **Phase 46 (dieser Plan): Karts-Tab.**
