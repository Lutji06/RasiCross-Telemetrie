# Phase 16 — Live + Detail v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructure `#tab-live` to a no-scroll visual-first layout (Karte big-left, Speed/RPM combo + big Rundenzeit + horizontal Sektoren + G/3D right) and expand `#tab-detail` with the moved 5-KPI row, deltaBanner, and a new Diagnose block.

**Architecture:** HTML/CSS reflow inside `RasiCross_Telemetry.html` + a small pure UMD helper (`dom-targets.js`) for the setter fan-out + JS wiring inside `rasicross.js`. No telemetry / protocol / firmware changes. Spec: `docs/superpowers/specs/2026-05-24-live-detail-v2-design.md`.

**Tech Stack:** Vanilla HTML/CSS/JS, UMD module pattern, `node:test` for pure logic, manual visual acceptance for DOM.

---

## Working Directory & Conventions

- Repo root: `C:\Users\jimlu\Documents\RasiCross-Telemetrie-git`.
- Branch: `feat/tab-redesign-pitwall` (already contains phase-15 work plus the LR/Pairing fixes from this session — commits `b394ce9`, `872f000`, `518ddae`).
- Files are **CRLF**: always `Read` the target region in-session immediately before any `Edit`, and copy the anchor from that fresh `Read`. Anchor on text — line numbers in this plan are indicative, not authoritative.
- Use the `Grep` tool (not shell `grep`) for verification asserts.
- Never `git add` `.claude/` or any plan/spec doc except the explicit plan-doc commit in the final task.
- Commit messages: conventional commit + body explaining the *why* + trailer `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>`.
- Per-task verification recipe (run before commit): `node --check rasicross.js` + `node --check dom-targets.js` (when applicable) + `npm test` (must stay green) + `python -m py_compile sender.py bridge.py` (no Python touched here, but keep green) + `python -m unittest discover -s test -p "test_*.py"`.
- Delete `__pycache__` and `**/__pycache__` before any `git status`.
- Baselines as of plan write (2026-05-24): `npm test` = **36** tests passing; `python -m unittest` baseline must remain green.

## Locked Decisions

- **Speed-Dial SVG / RPM-Bar deleted entirely.** Pure visualisation of `kSpeed`/`kRpm`, which both tabs already show numerically.
- **`gMeterCanvas` / `gMeter3dCanvas` / `gViewToggle` / `gxText` / `gyText` stay on Live**, relocated into their own `pw-module` inside the new right column.
- **ID-duplication policy:** suffix Live copies with `Live` (camelCase). `kSpeed`/`kRpm` stay on Detail; Live uses `kSpeedLive`/`kRpmLive`. Lap-time on Live uses new IDs `liveLapBig` / `liveLapBest` (not duplicates of `kLap`/`kLapBest`, because the visual is a big custom card not a KPI).
- **Setter fan-out** via a tiny pure UMD module `dom-targets.js` exposing `targetIdsFor(logicalKey) -> string[]`, unit-tested with `node:test`.
- **Diagnose block** is a single `pw-module` appended to `#tab-detail`, 2-col label/value grid, populated by a new `updateDiagnostics()` call inside the existing per-packet update path.
- **HTML script order** stays `geo.js → replay.js → dom-targets.js → rasicross.js`. New script tag added at line ~3116.

## File Structure

| Action | Path | Responsibility |
|---|---|---|
| Create | `dom-targets.js` | UMD module — `targetIdsFor(key)` pure lookup of logical key → DOM IDs |
| Create | `test/test-dom-targets.js` | `node:test` covering the lookup map |
| Modify | `RasiCross_Telemetry.html` | Live re-layout, Detail KPI-Strip, Detail Diagnose block, new CSS rules, new `<script>` tag |
| Modify | `rasicross.js` | Wire setter fan-out for shared values, add `updateDiagnostics()` |
| Create | `docs/superpowers/plans/2026-05-24-16-live-detail-v2.md` | This plan (committed in the final task) |

## Task Order

`Task 1 → Task 2 → Task 3 → Task 4 → Task 5 → Task 6 → Task 7 → Task 8`

Each task ends with its own commit. Final task commits this plan document.

---

## Task 1: `dom-targets.js` helper + unit test

**Files:**
- Create: `dom-targets.js`
- Create: `test/test-dom-targets.js`

- [ ] **Step 1: Write the failing test**

Create `test/test-dom-targets.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { targetIdsFor, SHARED_ID_MAP } = require('../dom-targets.js');

test('targetIdsFor: unknown key returns empty array', () => {
  assert.deepEqual(targetIdsFor('nope'), []);
});

test('targetIdsFor: speed fans out to Detail + Live IDs', () => {
  assert.deepEqual(targetIdsFor('speed'), ['kSpeed', 'kSpeedLive']);
});

test('targetIdsFor: rpm fans out to Detail + Live IDs', () => {
  assert.deepEqual(targetIdsFor('rpm'), ['kRpm', 'kRpmLive']);
});

test('targetIdsFor: speedMax / rpmMax / spdSrc each fan out to both tabs', () => {
  assert.deepEqual(targetIdsFor('speedMax'), ['kSpeedMax', 'kSpeedMaxLive']);
  assert.deepEqual(targetIdsFor('rpmMax'), ['kRpmMax', 'kRpmMaxLive']);
  assert.deepEqual(targetIdsFor('spdSrc'), ['spdSrcTag', 'spdSrcTagLive']);
});

test('targetIdsFor: lap fans out to Detail KPI + Live big card + Detail hero chip', () => {
  assert.deepEqual(targetIdsFor('lap'), ['kLap', 'liveLapBig', 'detailHeroLapCurrent']);
});

test('targetIdsFor: lapBest fans out to Detail KPI + Live sub + Detail hero chip', () => {
  assert.deepEqual(targetIdsFor('lapBest'), ['kLapBest', 'liveLapBest', 'detailHeroLapBest']);
});

test('SHARED_ID_MAP is a frozen plain object with string-array values', () => {
  assert.equal(typeof SHARED_ID_MAP, 'object');
  assert.equal(Object.isFrozen(SHARED_ID_MAP), true);
  for (const [k, v] of Object.entries(SHARED_ID_MAP)) {
    assert.equal(typeof k, 'string');
    assert.ok(Array.isArray(v) && v.length > 0, `${k} must be a non-empty array`);
    for (const id of v) assert.equal(typeof id, 'string');
  }
});
```

- [ ] **Step 2: Run test to verify it fails (module not found)**

```
node --test test/test-dom-targets.js
```

Expected: Failure — `Cannot find module '../dom-targets.js'`.

- [ ] **Step 3: Create `dom-targets.js` (UMD)**

```javascript
'use strict';
/*!
 * dom-targets.js — pure lookup of logical telemetry keys to DOM IDs.
 *
 * Why: Several values (Speed, RPM, lap time, …) live on both #tab-live and
 * #tab-detail. DOM IDs are unique per document, so the Live copies get an
 * "Live" suffix. Renderers in rasicross.js call setTextShared / setHtmlShared
 * which use this map to write to every relevant DOM node.
 *
 * Pure module — no DOM access, no side effects, safe to unit-test.
 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory();
  } else {
    root.DomTargets = factory();
  }
}(typeof self !== 'undefined' ? self : this, function () {

  const SHARED_ID_MAP = Object.freeze({
    speed:    ['kSpeed',    'kSpeedLive'],
    speedMax: ['kSpeedMax', 'kSpeedMaxLive'],
    spdSrc:   ['spdSrcTag', 'spdSrcTagLive'],
    rpm:      ['kRpm',      'kRpmLive'],
    rpmMax:   ['kRpmMax',   'kRpmMaxLive'],
    lap:      ['kLap',      'liveLapBig',  'detailHeroLapCurrent'],
    lapBest:  ['kLapBest',  'liveLapBest', 'detailHeroLapBest'],
  });

  function targetIdsFor(key) {
    const v = SHARED_ID_MAP[key];
    return v ? v.slice() : [];
  }

  return { SHARED_ID_MAP, targetIdsFor };
}));
```

- [ ] **Step 4: Run test to verify it passes**

```
node --test test/test-dom-targets.js
```

Expected: All 7 tests pass.

- [ ] **Step 5: Wire the script into the HTML**

In `RasiCross_Telemetry.html`, locate the script tags (around line 3115–3120):

```html
<script src="geo.js"></script>
<script src="replay.js"></script>
```

Insert a new line directly after `replay.js`:

```html
<script src="geo.js"></script>
<script src="replay.js"></script>
<script src="dom-targets.js"></script>
```

(The existing `<script src="rasicross.js"></script>` stays AFTER this new line.)

- [ ] **Step 6: Verify full test suite still green**

```
node --check rasicross.js
node --check dom-targets.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

Expected: `npm test` ≥ 36 → 43 tests passing (36 existing + 7 new). `python -m unittest` baseline unchanged.

- [ ] **Step 7: Commit**

```
git add dom-targets.js test/test-dom-targets.js RasiCross_Telemetry.html
git commit -m "feat(dashboard): add dom-targets helper for shared-ID setter fan-out

Pure UMD module returning the list of DOM IDs for each logical telemetry
key. Live and Detail tabs both display Speed/RPM/Lap; the Live copies use
a Live-suffix to keep IDs unique. Renderers in rasicross.js will switch
to this fan-out in a later task.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 2: New CSS rules for Live v2 layout + Detail Diagnose

**Files:**
- Modify: `RasiCross_Telemetry.html` (style block — search for `#tab-live .kpi{padding`)

- [ ] **Step 1: Locate the existing `#tab-live` scoped CSS**

Use Grep to find the anchor:

```
Grep pattern: "#tab-live #yawSparkCanvas{height:22px}"
```

The next blank/close-brace line is where we append.

- [ ] **Step 2: Append the new CSS rules**

Anchor on the existing block (read it first to copy the exact preceding line). Insert after `#tab-live .kpi-row{margin-bottom:12px}`:

```css
/* Live v2 — main body grid */
.pw-live-body{display:grid;grid-template-columns:2fr 1fr;gap:12px;align-items:stretch}
.pw-live-map{display:flex;flex-direction:column}
.pw-live-map .pw-module{flex:1 1 auto;display:flex;flex-direction:column}
.pw-live-map .map{flex:1 1 auto;min-height:0}
.pw-live-side{display:flex;flex-direction:column;gap:12px}
.pw-live-side .pw-module{padding:10px 12px}

/* Speed + RPM combo card */
.pw-kpi-combo{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.pw-kpi-combo .pw-kpi-cell{display:flex;flex-direction:column;gap:2px}
.pw-kpi-combo .pw-kpi-cell .kpi-l{font-size:10.5px;color:var(--mut);letter-spacing:.04em;text-transform:uppercase}
.pw-kpi-combo .pw-kpi-cell .kpi-v{font-size:clamp(24px,2.6vw,30px);font-weight:700;font-family:'JetBrains Mono',monospace}
.pw-kpi-combo .pw-kpi-cell .kpi-sub{font-size:10px;color:var(--mut)}

/* Big Rundenzeit card */
.pw-lap-big .kpi-v{font-size:clamp(40px,4.2vw,56px);font-weight:700;font-family:'JetBrains Mono',monospace;letter-spacing:.02em;line-height:1.05}
.pw-lap-big .kpi-sub{font-size:11px;color:var(--mut);margin-top:4px}

/* Sektoren horizontal strip inside the right column */
.pw-live-side #sectorPanel{display:flex;gap:8px}
.pw-live-side #sectorPanel .sector-card{flex:1 1 0;padding:8px 10px}
.pw-live-side #sectorPanel .sector-time{font-size:14px}
.pw-live-side #sectorPanel .sector-delta{font-size:11px}

/* G/3D card on Live — fills remaining vertical space */
.pw-live-side .pw-g3d{flex:1 1 auto;display:flex;flex-direction:column;align-items:center;justify-content:center}
.pw-live-side .pw-g3d canvas{max-width:100%;height:auto}

/* Detail Diagnose block — 2-col label/value grid */
.pw-diag-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px 24px}
.pw-diag-grid .pw-diag-row{display:flex;justify-content:space-between;gap:12px;padding:6px 0;border-bottom:1px dashed var(--bor)}
.pw-diag-grid .pw-diag-row:last-child{border-bottom:none}
.pw-diag-grid .pw-diag-label{font-size:11px;color:var(--mut);text-transform:uppercase;letter-spacing:.04em}
.pw-diag-grid .pw-diag-val{font-family:'JetBrains Mono',monospace;font-size:13px;color:var(--ink)}
```

- [ ] **Step 3: Verify new class names present exactly once each**

```
Grep pattern: "\.pw-live-body{"            → 1 match
Grep pattern: "\.pw-live-side .pw-module"  → 1 match
Grep pattern: "\.pw-kpi-combo{"            → 1 match
Grep pattern: "\.pw-lap-big "              → at least 1 match
Grep pattern: "\.pw-diag-grid{"            → 1 match
```

- [ ] **Step 4: Commit**

```
git add RasiCross_Telemetry.html
git commit -m "style(dashboard): add Live v2 + Detail Diagnose CSS scaffolding

New scoped rules for the two-column Live body (map left, stack right),
the Speed/RPM combo card, the big Rundenzeit display, the horizontal
Sektoren strip and the Detail Diagnose grid. Markup that uses these
classes lands in the next tasks.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 3: Live tab — remove old structure, add new `pw-live-body`

**Files:**
- Modify: `RasiCross_Telemetry.html` (around current lines 2155–2272 — KPI row, delta+sektor strip, map+tacho grid inside `#tab-live`)

This is the biggest single edit. Read the full target region before editing.

- [ ] **Step 1: Read the current Live tab body**

Read `RasiCross_Telemetry.html` lines roughly 2155–2274 to confirm the exact anchors of:
- KPI row (`<!-- KPIs -->` through `</div>` after `kpiBatt`).
- Delta+Sektor strip (`<!-- Delta + Sektor strip -->` through its closing `</div>`).
- Map+Tacho grid (`<!-- Map + gauges -->` through its closing `</div>`).
- The closing `</section>` of `#tab-live`.

- [ ] **Step 2: Replace the entire block KPI row → end of Map+Tacho grid with the new layout**

Old block starts at the `<!-- KPIs -->` comment and ends at the closing `</div>` of the `g-2-1` grid, right before `</section>` of `#tab-live`.

New replacement markup:

```html
  <!-- Delta + Sektoren strip is now split:
       deltaBanner moved to Detail KPI-Strip (Task 4);
       sectorPanel stays on Live inside the right-column stack below. -->

  <!-- Live v2 body: Karte (2/3) + right-column stack (1/3) -->
  <div class="pw-live-body">

    <!-- Karte (linke 2/3) -->
    <div class="pw-live-map">
      <div class="pw-module">
        <div class="pw-mod-head">
          <div class="pw-mod-title">Streckenkarte</div>
          <button class="heat-btn" id="heatmapBtn">Heatmap</button>
        </div>
        <div class="map"><canvas id="trackCanvas"></canvas></div>
        <div class="row" style="margin-top:14px;gap:10px">
          <div class="stat"><div class="t">Lat</div><div class="n" id="latText">--</div></div>
          <div class="stat"><div class="t">Lon</div><div class="n" id="lonText">--</div></div>
          <div class="stat"><div class="t">Punkte</div><div class="n" id="trackPoints">0</div></div>
          <div class="stat"><div class="t">Pakete</div><div class="n" id="packetsText">0</div></div>
        </div>
      </div>
    </div>

    <!-- Rechte 1/3 — Stack -->
    <div class="pw-live-side">

      <!-- Speed + RPM combo -->
      <div class="pw-module">
        <div class="pw-mod-head">
          <div class="pw-mod-title">Speed · RPM</div>
          <span class="card-sub">Live</span>
        </div>
        <div class="pw-kpi-combo">
          <div class="pw-kpi-cell">
            <span class="kpi-l">Speed</span>
            <span class="kpi-v" id="kSpeedLive">0<small>km/h</small></span>
            <span class="kpi-sub">Max <b id="kSpeedMaxLive">0</b> · <b id="spdSrcTagLive">GPS</b></span>
          </div>
          <div class="pw-kpi-cell">
            <span class="kpi-l">RPM</span>
            <span class="kpi-v" id="kRpmLive">0</span>
            <span class="kpi-sub">Max <b id="kRpmMaxLive">0</b></span>
          </div>
        </div>
      </div>

      <!-- Rundenzeit groß -->
      <div class="pw-module pw-lap-big">
        <div class="pw-mod-head">
          <div class="pw-mod-title">Rundenzeit</div>
          <span class="card-sub">aktuell</span>
        </div>
        <div class="kpi-v" id="liveLapBig">--:--.---</div>
        <div class="kpi-sub">Best <b id="liveLapBest">--:--.---</b></div>
      </div>

      <!-- Sektoren (horizontal) -->
      <div class="pw-module">
        <div class="pw-mod-head">
          <div class="pw-mod-title">Sektoren</div>
          <span class="card-sub">aktive Runde</span>
        </div>
        <div class="sector-panel" id="sectorPanel" style="display:none">
          <div class="sector-card" id="s1Card"><div class="sector-label">Sektor 1</div><div class="sector-time" id="s1Time">--:--.---</div><div class="sector-delta same" id="s1Delta">--</div></div>
          <div class="sector-card" id="s2Card"><div class="sector-label">Sektor 2</div><div class="sector-time" id="s2Time">--:--.---</div><div class="sector-delta same" id="s2Delta">--</div></div>
          <div class="sector-card" id="s3Card"><div class="sector-label">Sektor 3</div><div class="sector-time" id="s3Time">--:--.---</div><div class="sector-delta same" id="s3Delta">--</div></div>
        </div>
      </div>

      <!-- G-Meter / 3D Kart Toggle -->
      <div class="pw-module pw-g3d">
        <div class="pw-mod-head">
          <div class="pw-mod-title">G-Meter · 3D Kart</div>
          <span class="card-sub">Live</span>
        </div>
        <div class="g-view-toggle" id="gViewToggle">
          <button type="button" data-view="2d" class="active">2D</button>
          <button type="button" data-view="3d">3D</button>
        </div>
        <canvas id="gMeterCanvas"></canvas>
        <canvas id="gMeter3dCanvas" class="hidden"></canvas>
        <div class="g-readout">
          <span>Gx <b id="gxText">0.0</b></span>
          <span>Gy <b id="gyText">0.0</b></span>
        </div>
      </div>

    </div><!-- /pw-live-side -->
  </div><!-- /pw-live-body -->
```

- [ ] **Step 3: Verify deleted IDs are no longer on `#tab-live`**

```
Grep pattern: "id=\"speedDial\""    → 0 matches anywhere
Grep pattern: "id=\"speedArc\""     → 0 matches anywhere
Grep pattern: "id=\"rpmFill\""      → 0 matches anywhere
Grep pattern: "id=\"rpmScale\""     → 0 matches anywhere
Grep pattern: "id=\"speedGrad\""    → 0 matches anywhere
```

(The whole Tacho SVG is gone.)

- [ ] **Step 4: Verify preserved IDs still present on Live**

```
Grep pattern: "id=\"trackCanvas\""    → 1 match
Grep pattern: "id=\"sectorPanel\""    → 1 match
Grep pattern: "id=\"gMeterCanvas\""   → 1 match
Grep pattern: "id=\"gMeter3dCanvas\"" → 1 match
Grep pattern: "id=\"gViewToggle\""    → 1 match
Grep pattern: "id=\"gxText\""         → 1 match
Grep pattern: "id=\"gyText\""         → 1 match
Grep pattern: "id=\"s1Card\""         → 1 match
Grep pattern: "id=\"heatmapBtn\""     → 1 match
```

- [ ] **Step 5: Verify new Live-suffix IDs introduced exactly once**

```
Grep pattern: "id=\"kSpeedLive\""      → 1 match
Grep pattern: "id=\"kRpmLive\""        → 1 match
Grep pattern: "id=\"kSpeedMaxLive\""   → 1 match
Grep pattern: "id=\"kRpmMaxLive\""     → 1 match
Grep pattern: "id=\"spdSrcTagLive\""   → 1 match
Grep pattern: "id=\"liveLapBig\""      → 1 match
Grep pattern: "id=\"liveLapBest\""     → 1 match
```

- [ ] **Step 6: Verify `kSpeed`, `kRpm`, `kLap`, `deltaBanner` are NOT on Live anymore (still present elsewhere — Detail will own them in Task 4)**

Open Read on `#tab-live` (the `<section class="tab active" id="tab-live">` block up to its closing `</section>`) and confirm none of `id="kSpeed"`, `id="kRpm"`, `id="kLap"`, `id="kLapBest"`, `id="kG"`, `id="kpiBatt"`, `id="deltaBanner"` appear within that section.

Sanity-grep totals across the whole file (before Task 4 they should equal 1 because the old definitions still live elsewhere if any duplicated; the Live KPI row was the only occurrence, so they are now 0):

```
Grep pattern: "id=\"kSpeed\""        → 0 matches  (will become 1 after Task 4)
Grep pattern: "id=\"kRpm\""          → 0 matches  (will become 1 after Task 4)
Grep pattern: "id=\"kLap\""          → 0 matches  (will become 1 after Task 4)
Grep pattern: "id=\"deltaBanner\""   → 0 matches  (will become 1 after Task 4)
```

- [ ] **Step 7: Syntax / smoke check**

```
node --check rasicross.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

`npm test` count must equal Task 1 baseline (43 — no DOM-touching tests in this task).

- [ ] **Step 8: Commit**

```
git add RasiCross_Telemetry.html
git commit -m "feat(dashboard): rebuild Live tab as no-scroll Map+Stack layout

Drop the KPI row, the delta+sektor strip wrapper, the tacho SVG dial and
RPM bar from #tab-live. Replace the map+tacho grid with pw-live-body —
Karte fills the left 2/3 column, the right 1/3 stacks Speed/RPM combo,
big Rundenzeit, horizontal Sektoren and the G/3D toggle. Live-only
copies of kSpeed/kRpm/etc. get an Live suffix; Detail still owns the
canonical IDs (moved there in the next task).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 4: Detail tab — KPI-Strip block (5-KPI row + deltaBanner)

**Files:**
- Modify: `RasiCross_Telemetry.html` (inside `#tab-detail`, directly after the hero `</div>` and before `<!-- Verlauf …`)

- [ ] **Step 1: Locate insertion point**

Inside `#tab-detail`, the hero ends with the closing `</div>` of `.pw-hero`. The Verlauf grid starts with `<!-- Verlauf (Speed/RPM + G-Kraft) -->`. Insert the new KPI-Strip block between them.

- [ ] **Step 2: Insert the KPI-Strip block**

```html
  <!-- KPI-Strip: alle Live-Werte detailliert (war frueher auf #tab-live) -->
  <div class="kpi-row" id="detailKpiStrip" style="margin-bottom:12px">
    <div class="kpi accent">
      <div class="kpi-head">
        <span class="kpi-l">Speed</span>
        <span class="kpi-icon"><svg viewBox="0 0 24 24"><path d="M3 12a9 9 0 0 1 18 0M12 12l5-3"/></svg></span>
      </div>
      <div class="kpi-v" id="kSpeed">0<small>km/h</small></div>
      <div class="kpi-sub">Max <b id="kSpeedMax">0</b> · Quelle <b id="spdSrcTag">GPS</b></div>
    </div>
    <div class="kpi">
      <div class="kpi-head">
        <span class="kpi-l">RPM</span>
        <span class="kpi-icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l4 2"/></svg></span>
      </div>
      <div class="kpi-v" id="kRpm">0</div>
      <div class="kpi-sub">Max <b id="kRpmMax">0</b></div>
    </div>
    <div class="kpi">
      <div class="kpi-head">
        <span class="kpi-l">G-Kraft</span>
        <span class="kpi-icon"><svg viewBox="0 0 24 24"><path d="M3 6h18M3 12h18M3 18h18"/></svg></span>
      </div>
      <div class="kpi-v" id="kG">0.0<small>G</small></div>
      <div class="kpi-sub">Max <b id="kGMax">0.0</b> · Gier <b id="kYaw">0</b>°/s · MPU <b id="kMtemp">--</b>°C</div>
      <canvas id="yawSparkCanvas" width="240" height="28" style="width:100%;margin-top:6px;display:block"></canvas>
    </div>
    <div class="kpi">
      <div class="kpi-head">
        <span class="kpi-l">Aktuelle Runde</span>
        <span class="kpi-icon"><svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M2 12h4M18 12h4M5 5l3 3M16 16l3 3M5 19l3-3M16 8l3-3"/></svg></span>
      </div>
      <div class="kpi-v" id="kLap" style="font-size:clamp(30px,3.6vw,40px)">--:--.---</div>
      <div class="kpi-sub">Best <b id="kLapBest">--:--.---</b></div>
    </div>
    <div class="kpi hidden" id="kpiBatt">
      <div class="kpi-head">
        <span class="kpi-l">Batterie</span>
        <span class="kpi-icon"><svg viewBox="0 0 24 24"><rect x="2" y="7" width="16" height="10" rx="2"/><path d="M20 10v4"/></svg></span>
      </div>
      <div class="kpi-v" id="kBatt">--<small>%</small></div>
      <div class="kpi-sub" id="kBattSub"><b>--</b> V · Zelle <b>--</b> V</div>
    </div>
  </div>

  <!-- Delta-Banner — von Live nach Detail verschoben -->
  <div class="delta-banner hidden" id="deltaBanner" style="margin-bottom:18px">
    <span class="delta-label">Δ Live-Delta</span>
    <span class="delta-time same" id="deltaTime">+0.000s</span>
    <span class="delta-ref" id="deltaRef">vs. beste Runde</span>
  </div>
```

- [ ] **Step 3: Verify the moved IDs are now back, but only once**

```
Grep pattern: "id=\"kSpeed\""        → 1 match (in #tab-detail)
Grep pattern: "id=\"kRpm\""          → 1 match
Grep pattern: "id=\"kLap\""          → 1 match
Grep pattern: "id=\"kLapBest\""      → 1 match
Grep pattern: "id=\"kG\""            → 1 match
Grep pattern: "id=\"kGMax\""         → 1 match
Grep pattern: "id=\"kYaw\""          → 1 match
Grep pattern: "id=\"kMtemp\""        → 1 match
Grep pattern: "id=\"kpiBatt\""       → 1 match
Grep pattern: "id=\"kBatt\""         → 1 match
Grep pattern: "id=\"kBattSub\""      → 1 match
Grep pattern: "id=\"spdSrcTag\""     → 1 match
Grep pattern: "id=\"kSpeedMax\""     → 1 match
Grep pattern: "id=\"kRpmMax\""       → 1 match
Grep pattern: "id=\"yawSparkCanvas\"" → 1 match
Grep pattern: "id=\"deltaBanner\""   → 1 match
Grep pattern: "id=\"deltaTime\""     → 1 match
Grep pattern: "id=\"deltaRef\""      → 1 match
```

- [ ] **Step 4: Syntax / smoke check**

```
node --check rasicross.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

- [ ] **Step 5: Commit**

```
git add RasiCross_Telemetry.html
git commit -m "feat(dashboard): move 5-KPI row + deltaBanner to #tab-detail

The KPI row (Speed/RPM/G/Lap/Batt) and the deltaBanner — both removed
from Live in the previous task — land on Detail directly after the hero
as the new KPI-Strip block. DOM is moved verbatim; IDs unchanged so
existing rasicross.js setters keep working without code changes.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 5: Detail tab — Diagnose block

**Files:**
- Modify: `RasiCross_Telemetry.html` (inside `#tab-detail`, after the `detailLapsSlot` block, before the closing `</section>`)

- [ ] **Step 1: Locate insertion point**

The `detailLapsSlot` `<div>` ends with `</div>` followed by `</section>` of `#tab-detail`. Insert the new Diagnose `pw-module` between them.

- [ ] **Step 2: Insert the Diagnose block**

```html
  <!-- Diagnose: alle Roh-Werte der Telemetrie -->
  <div id="detailDiagnoseSlot" style="margin-top:18px">
    <div class="pw-module">
      <div class="pw-mod-head">
        <div class="pw-mod-title">Diagnose</div>
        <span class="card-sub">Roh-Werte · Funk · Sensoren</span>
      </div>
      <div class="pw-diag-grid">
        <div class="pw-diag-row"><span class="pw-diag-label">GPS Lat</span>      <span class="pw-diag-val" id="diagGpsLat">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">GPS Lon</span>      <span class="pw-diag-val" id="diagGpsLon">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">GPS Sat</span>      <span class="pw-diag-val" id="diagGpsSat">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">GPS Health</span>   <span class="pw-diag-val" id="diagGpsHealth">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Pakete</span>       <span class="pw-diag-val" id="diagPackets">0</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Lost</span>         <span class="pw-diag-val" id="diagLost">0</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Rate / s</span>     <span class="pw-diag-val" id="diagRate">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Link RSSI</span>    <span class="pw-diag-val" id="diagRssi">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Gx</span>           <span class="pw-diag-val" id="diagGx">0.00</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Gy</span>           <span class="pw-diag-val" id="diagGy">0.00</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Az</span>           <span class="pw-diag-val" id="diagAz">0.00</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Yaw °/s</span>      <span class="pw-diag-val" id="diagYaw">0</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">MPU °C</span>       <span class="pw-diag-val" id="diagMpuTemp">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Vbat</span>         <span class="pw-diag-val" id="diagVbat">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">SoC %</span>        <span class="pw-diag-val" id="diagSoc">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">Batt Warn</span>    <span class="pw-diag-val" id="diagBattWarn">0</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">send_ms</span>      <span class="pw-diag-val" id="diagSendMs">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">spd_src</span>      <span class="pw-diag-val" id="diagSpdSrc">--</span></div>
        <div class="pw-diag-row"><span class="pw-diag-label">imu_cal</span>      <span class="pw-diag-val" id="diagImuCal">--</span></div>
      </div>
    </div>
  </div>
```

- [ ] **Step 3: Verify all 19 diag IDs present exactly once each**

```
Grep pattern: "id=\"diag[A-Za-z]+\""  output_mode: "content"  -n: true
```

Expected: 19 unique lines, each `id="diagXxx"` exactly once. Spot-check a few:

```
Grep pattern: "id=\"diagGpsLat\""     → 1 match
Grep pattern: "id=\"diagRssi\""       → 1 match
Grep pattern: "id=\"diagSendMs\""     → 1 match
```

- [ ] **Step 4: Syntax / smoke check**

```
node --check rasicross.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

- [ ] **Step 5: Commit**

```
git add RasiCross_Telemetry.html
git commit -m "feat(dashboard): add Diagnose block to #tab-detail

A new pw-module appended to Detail shows every raw value the telemetry
packet carries: GPS (lat/lon/sat/health), packet stats (rx/lost/rate/
rssi), IMU (gx/gy/az/yaw/mtemp), battery (vbat/soc/warn), sender status
(send_ms/spd_src/imu_cal). Populated by updateDiagnostics() in the next
JS task.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 6: Wire setter fan-out for shared values in `rasicross.js`

**Files:**
- Modify: `rasicross.js` (the KPI update block around current lines 2185–2257)

- [ ] **Step 1: Add module-level helpers near `setText`**

Locate `setText` and `$` helper definitions in `rasicross.js` (top of the file). Directly below them add:

```javascript
// Shared-ID fan-out — Live and Detail share several values.
// DomTargets is provided by dom-targets.js (loaded before rasicross.js).
function setTextShared(key, value) {
  const ids = (typeof DomTargets !== 'undefined' && DomTargets.targetIdsFor)
    ? DomTargets.targetIdsFor(key) : [];
  for (const id of ids) setText(id, value);
}
function setHtmlShared(key, html) {
  const ids = (typeof DomTargets !== 'undefined' && DomTargets.targetIdsFor)
    ? DomTargets.targetIdsFor(key) : [];
  for (const id of ids) {
    const el = $(id);
    if (el) el.innerHTML = html;
  }
}
```

- [ ] **Step 2: Replace Speed setter**

Anchor: the existing block at lines ~2194–2198. Read it first to copy exact whitespace.

Old:
```javascript
    const speedText = _kpiDisplay.speed.toFixed(0);
    if (speedText !== _lastKpiText.speed) {
      $('kSpeed').innerHTML = `${speedText}<small>km/h</small>`;
      _lastKpiText.speed = speedText;
    }
```

New:
```javascript
    const speedText = _kpiDisplay.speed.toFixed(0);
    if (speedText !== _lastKpiText.speed) {
      setHtmlShared('speed', `${speedText}<small>km/h</small>`);
      _lastKpiText.speed = speedText;
    }
```

- [ ] **Step 3: Replace spdSrc setter (the colored chip)**

Find the block setting `spdSrcTag` (around lines ~2200–2211). Replace the `_srcEl.textContent = _srcLabel;` line with a fan-out and keep the styling on both nodes:

Old core:
```javascript
      const _srcEl = $('spdSrcTag');
      if (_srcEl) {
        _srcEl.textContent = _srcLabel;
        _srcEl.style.color = state.spdSrc === 'wheel' ? '#e8a13a'
                           : (state.spdSrc === 'none' || !state.spdSrc) ? 'var(--mut)'
                           : '';
      }
```

New core:
```javascript
      const _spdColor = state.spdSrc === 'wheel' ? '#e8a13a'
                      : (state.spdSrc === 'none' || !state.spdSrc) ? 'var(--mut)'
                      : '';
      for (const _id of DomTargets.targetIdsFor('spdSrc')) {
        const _e = $(_id);
        if (_e) { _e.textContent = _srcLabel; _e.style.color = _spdColor; }
      }
```

- [ ] **Step 4: Replace RPM setter**

Anchor lines ~2232–2235.

Old:
```javascript
    if (rpmText !== _lastKpiText.rpm) {
      setText('kRpm', rpmText);
      _lastKpiText.rpm = rpmText;
    }
```

New:
```javascript
    if (rpmText !== _lastKpiText.rpm) {
      setTextShared('rpm', rpmText);
      _lastKpiText.rpm = rpmText;
    }
```

- [ ] **Step 5: Replace Max-Werte setters**

Anchor lines ~2243–2244.

Old:
```javascript
    setText('kSpeedMax', state.max.speed.toFixed(0));
    setText('kRpmMax', Math.round(state.max.rpm / 50) * 50);
```

New:
```javascript
    setTextShared('speedMax', state.max.speed.toFixed(0));
    setTextShared('rpmMax', String(Math.round(state.max.rpm / 50) * 50));
```

- [ ] **Step 6: Replace Rundenzeit + Best-Lap setters**

Anchor lines ~2249–2257.

Old:
```javascript
    const lapText = state.lapStart ? fmtMs(Date.now() - state.lapStart) : '--:--.---';
    if (lapText !== _lastKpiText.lap) {
      setText('kLap', lapText);
      setText('detailHeroLapCurrent', lapText);
      _lastKpiText.lap = lapText;
    }
    const lapBestText = state.bestLapMs ? fmtMs(state.bestLapMs) : '--:--.---';
    setText('kLapBest', lapBestText);
    setText('detailHeroLapBest', lapBestText);
```

New:
```javascript
    const lapText = state.lapStart ? fmtMs(Date.now() - state.lapStart) : '--:--.---';
    if (lapText !== _lastKpiText.lap) {
      setTextShared('lap', lapText);   // -> kLap, liveLapBig, detailHeroLapCurrent
      _lastKpiText.lap = lapText;
    }
    const lapBestText = state.bestLapMs ? fmtMs(state.bestLapMs) : '--:--.---';
    setTextShared('lapBest', lapBestText);  // -> kLapBest, liveLapBest, detailHeroLapBest
```

- [ ] **Step 7: Verify no stray references to deleted IDs**

```
Grep pattern: "'speedDial'"   → 0 matches in rasicross.js
Grep pattern: "'speedArc'"    → 0 matches
Grep pattern: "'rpmFill'"     → 0 matches
Grep pattern: "'rpmScale'"    → 0 matches
```

If any match: surrounding code must be deleted as well. Speed-dial / RPM-bar SVG were live-updated from `rasicross.js`; that update code becomes dead and must be removed in this task. Use Grep to locate the surrounding `if` / function and delete the dead branch.

- [ ] **Step 8: Smoke check**

```
node --check rasicross.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

`npm test` total must still be 43.

- [ ] **Step 9: Commit**

```
git add rasicross.js
git commit -m "feat(dashboard): fan out shared KPI setters to Live + Detail copies

Speed/RPM/Max/spdSrc/lap/lapBest now route through DomTargets.targetIdsFor
so a single render call updates every relevant DOM node — kSpeed on
Detail, kSpeedLive on Live, etc. The dead Speed-Dial / RPM-Bar updaters
that pointed at the deleted SVG nodes are removed in the same commit.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 7: Add `updateDiagnostics()` in `rasicross.js`

**Files:**
- Modify: `rasicross.js` (define new function near the KPI update path; call site inside the existing per-packet update)

- [ ] **Step 1: Add the function**

Place near the existing KPI update function (anchor on the `function updateKPIs` or equivalent — Grep `kpiDisplay` / `_kpiDisplay` to find the surrounding function). Insert directly after that function:

```javascript
// Diagnose-Block auf #tab-detail — pro Paket geschrieben, schluckt
// fehlende Felder still (frühe Pakete bevor erste GPS-/Batt-/RSSI-Daten da sind).
function updateDiagnostics() {
  const t = state.telemetry || {};
  const b = state.batt || {};
  const imu = state.imu || {};
  const link = state.link || {};

  setText('diagGpsLat',    t.lat != null ? Number(t.lat).toFixed(6) : '--');
  setText('diagGpsLon',    t.lon != null ? Number(t.lon).toFixed(6) : '--');
  setText('diagGpsSat',    t.gps_sat != null ? String(t.gps_sat) : '--');
  setText('diagGpsHealth', t.gps_health || (t.gps_fix ? 'fix' : '--'));

  setText('diagPackets', String(state.packets || 0));
  setText('diagLost',    String(state.lost || 0));
  setText('diagRate',    state.pktRate != null ? state.pktRate.toFixed(1) : '--');
  setText('diagRssi',    link.rssi != null ? String(link.rssi) : '--');

  setText('diagGx',      (t.gx != null ? Number(t.gx).toFixed(2) : '0.00'));
  setText('diagGy',      (t.gy != null ? Number(t.gy).toFixed(2) : '0.00'));
  setText('diagAz',      (t.gz != null ? Number(t.gz).toFixed(2) : '0.00'));
  setText('diagYaw',     t.yaw != null ? String(Math.round(t.yaw)) : '0');
  setText('diagMpuTemp', imu.mtemp != null ? String(imu.mtemp) : (t.mtemp != null ? String(t.mtemp) : '--'));

  setText('diagVbat',    b.vbat != null ? b.vbat.toFixed(2) : '--');
  setText('diagSoc',     b.soc != null ? String(b.soc) : '--');
  setText('diagBattWarn',b.warn != null ? String(b.warn) : '0');

  setText('diagSendMs',  t.send_ms != null ? String(t.send_ms) : '--');
  setText('diagSpdSrc',  t.spd_src || '--');
  setText('diagImuCal',  t.imu_cal != null ? (t.imu_cal ? '1' : '0') : '--');
}
```

- [ ] **Step 2: Wire the call site**

Locate the per-packet update path — typically `updateKPIs()` or the function called once per inbound telemetry message. Grep for `updateKPIs(` to find call sites. At the end of that function (after the existing setters), add:

```javascript
  updateDiagnostics();
```

If `updateKPIs` is wrapped in a try/catch, place the `updateDiagnostics()` call inside the try block so a transient field shape mismatch doesn't break the rest of the render.

- [ ] **Step 3: Verify the wiring**

```
Grep pattern: "function updateDiagnostics\\("  → 1 match in rasicross.js
Grep pattern: "updateDiagnostics\\(\\)"        → at least 2 matches (definition + call)
```

- [ ] **Step 4: Smoke check**

```
node --check rasicross.js
npm test
python -m unittest discover -s test -p "test_*.py"
```

- [ ] **Step 5: Commit**

```
git add rasicross.js
git commit -m "feat(dashboard): populate Detail Diagnose block per packet

updateDiagnostics() reads state.telemetry / state.batt / state.imu /
state.link and writes 19 diag* spans in the Detail-tab Diagnose block.
Missing fields render as '--' rather than throwing — early packets
before GPS/RSSI/battery data arrive stay visually clean.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 8: Manual acceptance + plan-doc commit

- [ ] **Step 1: Manual acceptance checklist** (run the dashboard locally and walk through):

  - **Live tab @ 1440×900** shows no vertical scrollbar.
  - Hero chips: Rest-Rennzeit / Race-Meta / GPS update.
  - Race-Control row: all 5 buttons clickable, Start triggers Race state.
  - Karte: GPS trace renders, lat/lon/Punkte/Pakete update live.
  - Speed/RPM combo: both `kSpeedLive` and `kRpmLive` increment with telemetry.
  - Big Rundenzeit: `liveLapBig` ticks; `liveLapBest` updates when a new best lap finishes.
  - Sektoren strip: 3 cards horizontal, sector times + delta render after first sector.
  - G/3D toggle: 2D switches to 3D Kart, both canvases render.
  - **Detail tab** scrolls fine.
  - KPI-Strip on Detail (Speed/RPM/G/Lap/Batt) updates in lockstep with Live.
  - deltaBanner appears on Detail when delta-vs-best is live.
  - Verlauf (Speed/RPM + G charts) renders and updates.
  - Stints / Rundentabelle render.
  - Diagnose block fills with realistic values; missing fields show `--` not `undefined`.
  - **Tab-Switch Live ↔ Detail**: no canvas-resize glitches; second switch shows fresh data.
  - **No CSP violation** in the DevTools console during a full race cycle (start → laps → end).

- [ ] **Step 2: Re-baseline checks before final commit**

```
node --check rasicross.js
node --check dom-targets.js
npm test
python -m unittest discover -s test -p "test_*.py"
python -m py_compile sender.py bridge.py esp_libs/*.py
```

Delete `__pycache__` directories before `git status`.

- [ ] **Step 3: Commit the plan document**

```
git add docs/superpowers/plans/2026-05-24-16-live-detail-v2.md
git commit -m "docs: phase 16 implementation plan (Live + Detail v2)

Eight-task plan that rebuilds #tab-live as a no-scroll Map+Stack layout
and expands #tab-detail with the moved 5-KPI row, deltaBanner and a new
Diagnose block. Pure HTML/CSS/JS — no telemetry, protocol or firmware
changes.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

- [ ] **Step 4: Push the branch**

```
git push
```

Expect a clean push (only fast-forward); branch already tracks origin.

---

## Self-Review

**Spec coverage:**
- §4.1 vertical budget — addressed by Live re-layout (Task 3) + CSS (Task 2). The CSS grid (`grid-template-columns: 2fr 1fr`) and right-column flex stack are in Task 2.
- §4.2 Live structural changes — Task 3.
- §4.3 deleted elements — Task 3 (steps 3 and 7).
- §4.4 Detail additions — KPI-Strip (Task 4), unchanged Verlauf/Stints/Lap-Tabelle (no-op for this plan, already exist from phase 15), Diagnose (Task 5).
- §4.5 `rasicross.js` changes — setter fan-out (Task 6), `updateDiagnostics` (Task 7).
- §4.6 ID-duplication policy — encoded in `dom-targets.js` (Task 1) and Live markup (Task 3).
- §4.7 CSS additions — Task 2.
- §5 risks — ID coupling addressed via the suffix map; render-on-hidden inherited from phase 15; CSP unchanged (no new inline handlers).
- §6 testing strategy — Task 1 covers the unit test for the helper; Task 8 covers the manual acceptance + green baseline.

**Placeholder scan:** none — every step has the literal code or command needed.

**Type / name consistency:**
- `targetIdsFor(key)` used in Tasks 1, 6.
- `setTextShared` / `setHtmlShared` defined Task 6 Step 1, used in Task 6 Steps 2, 4, 5, 6.
- `updateDiagnostics` defined Task 7 Step 1, called Task 7 Step 2.
- DOM IDs match across Tasks 1 (map), 3 (Live markup), 4 (Detail markup), 6 (setters): `kSpeed`/`kSpeedLive`, `kRpm`/`kRpmLive`, `kSpeedMax`/`kSpeedMaxLive`, `kRpmMax`/`kRpmMaxLive`, `spdSrcTag`/`spdSrcTagLive`, `kLap`/`liveLapBig`, `kLapBest`/`liveLapBest`, plus `detailHeroLapCurrent`/`detailHeroLapBest` already present from phase 15.

## Phase Map

- P1 = B1+B2, P2 = A1+A2, P3 = A5, P4 = A3, P5 = A4, P6 = C1
- P11 = 3D Kart G-Viewer
- P12 = 3D Kart Custom-Modell Upload
- P13 = race-run toggle
- P14 = Pit-Wall tab redesign
- P15 = Live-tab no-scroll + Detail-tab v1
- **P16 = Live + Detail v2 (this plan)**
