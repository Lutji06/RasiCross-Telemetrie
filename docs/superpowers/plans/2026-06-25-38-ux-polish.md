# Phase 38 — UX/Design-Polish Implementation Plan

> Ausgeführt inline (kleine, risikolose CSS/Markup/Copy-Änderungen nach einem vollständigen UX-Review inkl. visuellem Electron-Durchgang).

**Goal:** Die im Review gefundenen A11y-/Ergonomie-/Copy-Lücken schließen, ohne Optik oder Logik zu verändern.

**Architecture:** Reines Frontend — CSS + Markup in `RasiCross_Telemetry.html`, eine Zeile JS in `rasicross.js` (aria-current beim Tab-Wechsel), Copy in `races.js`/`laps-drivers.js`.

**Tech Stack:** Vanilla JS/CSS, Electron (Chromium). Verifikation: `node --check`, `eslint`, `node --test` (Regressionswächter), Playwright-`_electron`-Screenshots.

**Spec:** `docs/superpowers/specs/2026-06-25-ux-polish-design.md`

## Working Directory & Conventions

- Repo-Root: `C:/Users/jimlu/Documents/RasiCross-Telemetrie-git`. Branch: `feat/phase-38-ux-polish` (von `main` nach Phase 37).
- **CRLF.** Vor jedem Edit frisch lesen, auf Text ankern. Grep-Tool zur Verifikation.
- **Niemals `.claude/` committen**, Doc-Commit zuletzt. Trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Additiv/abwärtskompatibel; keine Logik-/Telemetrie-Änderung → `node --test` bleibt 173.

## File Structure

| Aktion | Datei | Verantwortung |
|--------|-------|---------------|
| Ändern | `RasiCross_Telemetry.html` | `:focus-visible`-Ring, Coarse-Pointer-44px, `<nav>`+`aria-current`, Hamburger-`aria-label`, Map-Layers-Icon (+CSS), Leerzustand-Platzhalter. |
| Ändern | `rasicross.js` | `aria-current` beim Tab-Wechsel setzen/entfernen. |
| Ändern | `races.js` | Handlungsleitender Leerzustand „Rennen". |
| Ändern | `laps-drivers.js` | Handlungsleitender Leerzustand „Fahrer". |

## Tasks (alle ausgeführt)

- [x] **A — Fokus-Ring (CSS).** Globaler `:focus-visible`-Outline (Lime; Outdoor 3 px schwarz); Inputs/Selects behalten box-shadow-Ring.
- [x] **B — Nav-Semantik + Hamburger-Labels.** `<nav aria-label="Hauptnavigation">` um beide nav-groups; `aria-current="page"` initial am aktiven Live-Tab + JS pflegt es beim Wechsel; `aria-label` an `#openMobileBtn`/`#closeMobileBtn`.
- [x] **C — Touch-Ziele.** `@media (pointer:coarse)`: `.btn.icon-only` → 44×44 px; `.map-toggle`/`.heat-btn` min-height 40.
- [x] **D — Map-Toggle-Icon.** „M" → Layers-SVG; `.map-toggle` auf `grid;place-items:center` + svg-Regel.
- [x] **E — Leerzustände.** Rennen/Fahrer (JS + HTML-Platzhalter) und Strecke-Subtitle handlungsleitend formuliert.
- [x] **Verifikation.** `node --check` + `eslint` (rasicross/races/laps-drivers) OK; `node --test` 173 PASS; Electron-Re-Check: Fokus-Ring sichtbar, Map-Icon da, `aria-current` folgt Tab, `nav[aria-label]` da.
- [ ] **Doc-Commit.** Spec + Plan.

## Verworfen (kein Bug)

- **„Outdoor-Karte hell erzwingen":** Dunkelheit stammt aus der nutzergewählten Tile-URL, nicht aus dem Theme (`paintTilesOn` malt Tiles nur bei aktiviertem Karten-Hintergrund; sonst Basis-Fill `--soft` = in Outdoor hell). Ein Override würde die Karten-Wahl des Nutzers übersteuern. Details: Spec §2.

## Hardware/Manual Acceptance

Rein visuell, am Desktop verifiziert. Optional am Gerät: Tab-Durchlauf zeigt überall den Fokus-Ring; Mobil/Touch fühlt sich treffsicherer an; Outdoor-Fokus-Ring (schwarz) in Sonne sichtbar.

## Self-Review

- **Coverage:** Befunde 1–6 → Tasks A–E; Verworfenes dokumentiert (Spec §2).
- **Konsistenz:** Leerzustand-Copy in JS-Render UND HTML-Platzhalter synchron; `aria-current` initial (Markup) + dynamisch (JS).
- **Kein Logikbruch:** `node --test` unverändert 173; `liveTileToggle`-JS togglet nur `.off`, überschreibt das SVG nicht.

## Phase Map

- **Phase 36:** Streckenabstand-Gap. **Phase 37:** MPU-9250 + GPS 5 Hz.
- **Phase 38 (dieser Plan):** UX/Design-Polish (A11y, Touch, Copy, Map-Icon).
- **Phasen 39+ (deferred):** „Auto"-Tile-Stil (theme-folgend); AK8963-Heading-Spike; synchrones Multi-Kart-Replay.
