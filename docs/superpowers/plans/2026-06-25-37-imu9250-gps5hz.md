# Phase 37 — MPU-9250 + NEO-M8N (5 Hz) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Firmware an den Hardware-Umbau anpassen — IMU MPU-6050→MPU-9250 (nur Temperatur-Kennlinie), GPS →NEO-M8N auf 5 Hz (RMC+GGA, 9600 Baud). Accel/Gyro/lat/lon bleiben unverändert.

**Architecture:** Zwei reine, getestete Funktionen in `esp_libs/calc.py` (`mpu9250_temp_c`, `ubx_frame`). Der IMU-Treiber (`mpu6050.py`) nutzt die Temp-Funktion; `sender.py` GPS-Klasse sendet beim Boot UBX-Konfig.

**Tech Stack:** MicroPython (ESP32) / CPython (Tests), `unittest`. Verifikation: `py_compile`, `unittest`, `node --check`/`node --test` (JS unverändert — Regressionswächter).

**Spec:** `docs/superpowers/specs/2026-06-25-imu9250-gps5hz-design.md`

## Working Directory & Conventions

- Repo-Root: `C:/Users/jimlu/Documents/RasiCross-Telemetrie-git`. Branch: `feat/phase-37-imu9250-gps5hz` (von `main` nach Phase 36).
- **Dateien sind CRLF.** Vor jedem `Edit` Zielstelle frisch lesen, Anker daraus kopieren. Verifikation mit dem **Grep-Tool**.
- **Niemals `.claude/` committen**, Plan-/Spec-Doc nur im finalen Doc-Commit (Task 4).
- Commit-Trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- `__pycache__` vor jedem `git status`/commit löschen (außer `node_modules`).
- **Backward-compatible / additiv:** keine neuen Telemetriefelder, ESP-NOW-Budget unberührt; `bridge.py` + JS bleiben unverändert.

## Locked decisions

- Alle Karts sind 9250 → Temp-Formel **fest** auf 9250 (keine Laufzeit-Erkennung).
- IMU-Einbaulage unverändert → **keine** Achs-Umverdrahtung.
- GPS bleibt bei **9600 Baud**; 5 Hz wird durch Eindampfen auf RMC+GGA möglich (keine fragile Boot-Baud-Umstellung).
- Treiber-Datei-/Klassenname bleiben `mpu6050`/`MPU6050` (Accel/Gyro register-kompatibel; Import + Flash-Prozedur unangetastet).

## File Structure

| Aktion | Datei | Verantwortung |
|--------|-------|---------------|
| Ändern | `esp_libs/calc.py` | Neue reine `mpu9250_temp_c(raw)` + `ubx_frame(cls, id, payload)`. |
| Ändern | `test/test_calc.py` | `unittest`-Fälle für beide. |
| Ändern | `esp_libs/mpu6050.py` | `temperature_c` auf 9250 (via calc, Inline-Fallback); Header-Kommentar. |
| Ändern | `sender.py` | GPS-Klasse: `_configure_m8n()` sendet UBX (5 Hz, RMC+GGA) beim Init. |

**Task-Reihenfolge:** 1 (calc + Tests, TDD) → 2 (IMU-Treiber Temp) → 3 (sender GPS-Konfig) → 4 (finale Verifikation + Docs).

## Task 1: `calc.py` — `mpu9250_temp_c` + `ubx_frame` (Pure-Logik, TDD)

- [x] Tests in `test/test_calc.py` ergänzen (failing): `Mpu9250TempC` (Stützpunkte, ≠6050, bad→0.0) + `UbxFrame` (CFG-RATE 5 Hz `b5..de6a`, CFG-MSG GSV-aus `b5..fd15`, Leer-Payload-Header/Länge).
- [x] `unittest` → FAIL (Funktionen fehlen).
- [x] Funktionen in `calc.py` implementieren (Fletcher-Checksumme, NaN-Guards).
- [x] `py_compile calc.py` + `unittest` → **56 OK** (vorher 50).
- [x] Commit: `feat(imu-gps): mpu9250_temp_c + ubx_frame pure helpers + tests`.

## Task 2: `mpu6050.py` — Temperatur auf 9250

- [x] Header-Kommentar: Treiber bedient jetzt 9250 (Accel/Gyro kompatibel, nur Temp anders). `import calc` mit `_HAS_CALC`-Guard.
- [x] `temperature_c`: `calc.mpu9250_temp_c(raw)` wenn vorhanden, sonst Inline-`raw/333.87 + 21.0`.
- [x] `py_compile mpu6050.py`.
- [x] Commit: `feat(imu-gps): MPU-9250 temperature curve in IMU driver`.

## Task 3: `sender.py` — NEO-M8N 5 Hz

- [x] GPS-`__init__`: nach `self._ok = True` `self._configure_m8n()` aufrufen.
- [x] `_configure_m8n()`: CFG-MSG GLL/GSA/GSV/VTG→0, GGA/RMC→1, CFG-RATE 200 ms; `_HAS_CALC`-Guard; try/except (fire-and-forget).
- [x] `py_compile sender.py`; Grep `_configure_m8n`/`calc.ubx_frame` vorhanden.
- [x] Commit: `feat(imu-gps): configure NEO-M8N to 5 Hz (RMC+GGA) at boot`.

## Task 4: Finale Verifikation + Docs

- [ ] `python -m py_compile sender.py bridge.py esp_libs/*.py` → ok.
- [ ] `python -m unittest discover -s test -p "test_*.py"` → **56 OK**.
- [ ] `node --check geo.js lap-engine.js kart-overview.js map-draw.js` + `node --test` → **173** (JS-Regressionswächter, unverändert).
- [ ] `__pycache__` löschen, `git status` → sauber bis auf `.claude/` + Docs.
- [ ] Spec + Plan committen.

## Hardware/Manual Acceptance Checklist (an Nutzer — am Kart)

1. **IMU:** flashen → Diag-Seite zeigt plausible MPU-Temp (~Umgebungstemp, nicht ~+15 °C zu hoch); Drift/Rollover/G-Meter reagieren wie vorher (Achsen korrekt).
2. **GPS 5 Hz:** Marker/Position aktualisieren sichtbar flüssiger; Phase-36-Meter-Gap läuft glatter. Mit `DEBUG`/init-Log: „NEO-M8N auf 5 Hz konfiguriert".
3. **GPS-Fix:** lat/lon/speed/fix wie gewohnt (GNRMC/GNGGA).
4. **Fallback:** Modul ohne calc.py → 1 Hz, kein Crash.

## Self-Review

- **Spec-Coverage:** §3 `mpu9250_temp_c`/`ubx_frame` → Task 1; Treiber → Task 2; GPS-Konfig → Task 3. §4 Randfälle: kein calc (Task 2 Fallback + Task 3 Guard), fire-and-forget (Task 3 try/except). §5 Tests → Task 1.
- **Placeholder-Scan:** keine TBD/TODO; vollständiger Code committet.
- **Typ-/Namens-Konsistenz:** `mpu9250_temp_c(raw)`/`ubx_frame(cls,id,payload)` (Task 1 def; Task 2/3 Nutzung). Accel/Gyro-API unverändert → Dashboard/`bridge.py` unberührt.

## Phase Map

- **Phase 36:** Momentaner Streckenabstand-Gap (Meter) + fortschrittsbasierter Tiebreak.
- **Phase 37 (dieser Plan):** Hardware-Nachzug MPU-9250 (Temp) + NEO-M8N 5 Hz.
- **Phasen 38+ (deferred):** AK8963-Magnetometer-Heading; synchrones Multi-Kart-Replay; Pit-Wall-Fahrername-Fix.
